# LinkShrink — Epic Plan

Source TDD: [.development-docs/LinkShrink_TDD.md](.development-docs/LinkShrink_TDD.md)

This breaks the TDD into small, independently deliverable epics. Backend foundation
and the shared package come first; each service is layered on top; the frontend and
infra wire everything together; integration testing and observability close it out.
Every epic is sized to a single reviewable commit and leaves `development` working
(its own tests/lint pass and nothing previously merged breaks).

---

## Epic 1 — Repo & tooling scaffold — COMPLETED
- **Intent:** Stand up the monorepo skeleton so every later epic has a home, shared tooling, and a one-command lint/test entry point.
- **Scope:** Directory layout from §5.1 (`packages/shared/`, `services/{api,redirect,worker}/`, `frontend/`, `migrations/`, `infra/`); root `pyproject.toml` with ruff + pytest config; `linkshrink_shared` package skeleton (empty modules `models.py`, `shortcode.py`, `validation.py`, `config.py`, `cache.py`, `queue.py` with docstrings); `.env.example` (hashids salt, PG creds, Redis creds, public host); `.gitignore` entries for `.env`; base `README.md`; base Dockerfile(s) for the Python services (a common base image whose layer installs the editable `linkshrink_shared` package, reused by api/redirect/worker images per §9.1).
- **Verification:** `ruff check` and `pytest` run clean (no tests yet, exit 0); `pip install -e packages/shared` succeeds and `import linkshrink_shared` works; `docker build` of the base Python image succeeds with `linkshrink_shared` importable inside it.
- **Depends on:** none.
- **Implementation notes (delivered):**
  - **Python 3.12**, plain pip + **setuptools** build backend (no uv/poetry), matching the TDD's `pip install -e` flow.
  - The shared package uses a **src layout** — `packages/shared/src/linkshrink_shared/` (refines the TDD §5.1 flat sketch) — to keep editable installs clean and prevent importing an uninstalled copy. Its modules (`models/shortcode/validation/config/cache/queue.py`, plus `__init__.py` with `__version__`) are docstring-only stubs naming the epic that fills each.
  - **Shared runtime deps declared now** in `packages/shared/pyproject.toml`: `sqlalchemy[asyncio]>=2.0,<2.1`, `asyncpg>=0.29`, `pydantic-settings>=2`, `redis>=5`, `hashids>=1.3`. Service-specific deps (fastapi, uvicorn, qrcode, user-agents) are deferred to their own epics since `services/*` are empty placeholders (`.gitkeep`).
  - Root `pyproject.toml` is **tooling-config only** (no `[project]` table): `[tool.ruff]` (target py312, line-length 100, rule set E/F/I/UP/B) and `[tool.pytest.ini_options]` (`asyncio_mode = "auto"`). Dev/tooling deps live in `requirements-dev.txt` (`ruff`, `pytest`, `pytest-asyncio`, `alembic`, `testcontainers[postgres,redis]`).
  - Added `tests/test_smoke.py` (imports `linkshrink_shared`, asserts `__version__`). **Reason:** `pytest` with zero collected tests exits **5**, not 0 — one trivial import test satisfies both the "pytest exit 0" *and* "`import linkshrink_shared` works" checks. (The "no tests yet" wording is met by a single smoke test.)
  - Base image lives at `infra/docker/python-base.Dockerfile` (`python:3.12-slim`, `COPY packages/shared` + editable install), built from the repo root: `docker build -f infra/docker/python-base.Dockerfile -t linkshrink-base .`. Epic 18a's api/redirect/worker images build `FROM` it.
  - Also added `.gitignore` (`.env`, Python/build caches, `node_modules`, `dist/build`), `.env.example` (hashids salt, PG/Redis creds, `PUBLIC_HOST`), and base `README.md` (layout + dev-setup + base-image build). `.gitignore` ignores `.env` exactly, so the tracked `.env.example` is preserved.
  - **Added in review:** `.dockerignore` and `.gitattributes`.
    - `.dockerignore` — the base image builds from the repo root, so the whole repo is the build context; this excludes `*.egg-info/`, `__pycache__/`, `.venv/`, `node_modules/`, `.git/`, etc. so host build cruft is neither shipped to the daemon nor copied into the shared editable-install layer.
    - `.gitattributes` — `* text=auto eol=lf` (plus explicit `*.sh text eol=lf`) so text files commit as LF and never leak CRLF into the Linux service containers, ahead of the shell health-check scripts arriving in Epics 12/18a. **Caveat:** this normalizes on next touch only; run `git add --renormalize .` if a full-tree normalization is ever wanted.
  - **Verified:** editable install + `import linkshrink_shared` ✓; `ruff check .` exit 0 ✓; `pytest` exit 0 (1 passed) ✓; `docker build` of the base image + in-container `import linkshrink_shared` ✓.

## Epic 2 — Database models & Alembic migration — COMPLETED
- **Intent:** Define the source-of-truth schema so all services share one set of models.
- **Scope:** SQLAlchemy 2.0 models `Link` and `ClickEvent` in `shared/models.py` (§5.3) with enums for `device_type` and `source`; the `link_code_seq` sequence; unique functional index on `lower(short_code)`, composite `(created_at DESC, id DESC)`, `expires_at`, and `(link_id, clicked_at)` indexes; `ON DELETE CASCADE` FK; Alembic env + initial forward-only migration in `migrations/`.
- **Verification:** `alembic upgrade head` against a local/Testcontainers Postgres creates tables, sequence, and all indexes; `alembic downgrade base` then `upgrade head` round-trips; a quick model insert/select sanity test passes.
- **Depends on:** Epic 1.
- **Note:** the initial migration implements a real `downgrade()` (trivially drops everything back to an empty DB — lossless by definition for the first revision), used only for the round-trip consistency check above and local test teardown. This does **not** contradict the "forward-only" policy (§5.11/§8), which means *downgrades are never run in staging/production* — to undo a shipped change you write a new forward migration. See the clarified §5.11/§8 wording.
- **Implementation notes (delivered):**
  - **Models** in `packages/shared/src/linkshrink_shared/models.py`: SQLAlchemy 2.0 declarative `Base` carrying a shared `MetaData` with a **naming convention** (`ix/uq/ck/fk/pk` templates) so ORM- and migration-side constraint/index names always agree. `Link` and `ClickEvent` use `Mapped[...]` + `mapped_column`; both PKs are `BigInteger` + `Identity()` → `GENERATED BY DEFAULT AS IDENTITY` (TDD's "identity", not serial). Exported from `__init__.py` so `from linkshrink_shared import Link, ClickEvent, DeviceType, Source, link_code_seq` works.
  - **Enums are native PostgreSQL enum types** (`device_type`, `source`), not varchar+CHECK — matches the TDD wording. Modeled as `enum.StrEnum` subclasses (`DeviceType`, `Source`); values are fixed so there is no `ALTER TYPE` evolution concern.
  - **`link_code_seq`** is a standalone `Sequence` attached to `Base.metadata` (not bound to a column) — it feeds hashids in Epic 3 via `nextval('link_code_seq')`.
  - **Functional/DESC index** decision: the `lower(short_code)` unique index and the `(created_at DESC, id DESC)` index are declared with `sa.text(...)` in the model `__table_args__` and emitted via raw `op.execute(CREATE [UNIQUE] INDEX ...)` in the migration, because Alembic autogenerate does not reliably reproduce functional indexes or per-column `DESC` ordering. The migration is therefore **hand-authored** (not autogenerated) so the DDL matches §5.3 exactly (verified via `alembic upgrade head --sql`).
  - **Alembic env is async** (`migrations/env.py`, `asyncio.run(run_async_migrations())`) because the shared package ships **only `asyncpg`** — no sync driver (psycopg2). `alembic.ini` lives at the **repo root** (`script_location = migrations`, `path_separator = os`), with a zero-padded sortable `file_template`.
  - **DB URL resolution forward-reference:** `config.py` (pydantic-settings) is Epic 5, so `env.py` resolves the URL itself — an explicit `sqlalchemy.url` set on the `Config` (used by the tests) else a `postgresql+asyncpg://` URL built directly from the `POSTGRES_*` env vars (those in `.env.example`). This direct env read is a deliberate Epic-2-era stand-in; Epic 5 may later route it through shared config.
  - **Migration `downgrade()`** reverses in dependency order: drop indexes → drop tables → `DROP SEQUENCE` → drop the two enum types (the enum types are created explicitly with `checkfirst` and the columns use `create_type=False`, so type creation/teardown order is deterministic).
  - **Tests** (`tests/test_migrations.py`, Testcontainers Postgres `postgres:16-alpine`): `upgrade head` then assert the two tables, the sequence, and all four indexes exist; `downgrade base → upgrade head` round-trip; ORM insert/select of a `Link` + `ClickEvent`, `nextval('link_code_seq')` usability, and a **core `DELETE FROM links`** to prove the DB-level `ON DELETE CASCADE` (not just ORM cascade). Alembic is driven from **sync** fixtures/tests (env.py's `asyncio.run` cannot run inside a live loop); data assertions use a fresh async engine. The module **`pytest.skip`s if Docker is unavailable** so `pytest` still exits 0 on a Docker-less machine (matching Epic 1's clean-run expectation).
  - Removed the now-stale `migrations/.gitkeep` (the directory has real content).
  - **Verified:** `ruff check .` exit 0 ✓; `pytest` 4 passed (smoke + 3 migration tests) against a real PG container ✓; `alembic upgrade head --sql` emits the exact §5.3 DDL (native enums, IDENTITY PKs, timestamptz `now()` defaults, cascade FK, functional `lower(short_code)` unique + `(created_at DESC, id DESC)` indexes) ✓.

## Epic 3 — Short-code generation (shared) — COMPLETED
- **Intent:** One authoritative, deterministic short-code generator with collision retry.
- **Scope:** `shared/shortcode.py` — hashids encode/decode using env salt, single-case alphabet, min length 6 (§5.4); `nextval('link_code_seq')` consumption; bounded conflict-retry loop (advance sequence + re-encode on unique violation, sane cap, logs) per §7.
- **Verification:** Unit tests: encode is deterministic per `n`, decode round-trips, output matches alias grammar `[a-z0-9-]` (single case) with length ≥ 6, retry loop advances on simulated conflict and gives up after the cap.
- **Depends on:** Epic 2.
- **Implementation notes (delivered):**
  - **Alphabet** is `SHORT_CODE_ALPHABET = "abcdefghijklmnopqrstuvwxyz0123456789"` (36 lowercase letters + digits, **no hyphen**) with `SHORT_CODE_MIN_LENGTH = 6`. hashids only emits characters from its alphabet — and its internal separators/guards for this alphabet are themselves drawn from it (verified against hashids 1.3: separators `shufticab`, guards `85l`, all `[a-z0-9]`) — so a generated code is **always `[a-z0-9]` ⊂ alias grammar `[a-z0-9-]`**, single-case, length ≥ 6. The hyphen the grammar allows is for *custom* aliases (Epic 4), never for generated codes, so it is deliberately absent from the alphabet.
  - **`ShortCodeGenerator`** wraps a `Hashids(salt, alphabet, min_length)` instance: `encode(n)` (guards `n < 1` → `ValueError`, since hashids silently returns `""` for non-positive `n` which would break the length-6 invariant; `nextval` starts at 1 so this never fires in practice) and `decode(code) -> int | None` (`hashids.decode` returns a tuple → first element or `None`). Construction rejects an empty salt.
  - **Retry contract is a bool callback.** `generate_unique_short_code(get_next_sequence_value, try_persist, *, generator=None, max_attempts=DEFAULT_MAX_ATTEMPTS)` is fully decoupled from the DB (no SQLAlchemy import), which is what lets the retry path be **unit-tested with no container**. `try_persist(code)` returns `True` on success / `False` only on the `lower(short_code)` unique violation (retry), and MUST let every other error propagate so a transient failure is never silently retried as a collision. Epic 6 supplies the adapter that wraps its `INSERT` in `session.begin_nested()` (savepoint) and translates only the `uq_links_lower_short_code` `IntegrityError` to `False`.
  - **`DEFAULT_MAX_ATTEMPTS = 5`** — a safety cap, not an expected limit: collisions only occur against a pre-existing custom alias and `nextval` advances on every call, so consecutive collisions are effectively impossible. Each retry logs a `WARNING`; exhausting the cap raises `ShortCodeCollisionError`. This establishes the shared-package logging convention (`logger = logging.getLogger(__name__)`).
  - **Salt source is a deliberate Epic-3 stand-in.** `default_short_code_generator()` reads `HASHIDS_SALT` directly from the environment (raising `RuntimeError` if unset/empty), mirroring Epic 2's direct env read in `migrations/env.py`. Epic 5's `config.py` (pydantic-settings, still a stub) will later route this through shared config. Tests inject a fixed salt and never touch the real env.
  - **`fetch_next_sequence_value(session)`** is a thin `await session.scalar(link_code_seq.next_value())` wrapper (ties to the `link_code_seq` model object). It needs a live DB, so it is **not** unit-tested here — the `nextval` SQL is already proven against a real container in `tests/test_migrations.py`, and the wrapper is exercised end-to-end through the API insert path in Epic 6 / the integration suite in Epic 19. This keeps Epic 3 unit-only per its acceptance criteria.
  - **Exports** added to `linkshrink_shared.__init__`: `ShortCodeGenerator`, `ShortCodeCollisionError`, `generate_unique_short_code`, `fetch_next_sequence_value`, `default_short_code_generator`, `SHORT_CODE_ALPHABET`, `SHORT_CODE_MIN_LENGTH`, `DEFAULT_MAX_ATTEMPTS`.
  - **Tests** in `tests/test_shortcode.py` (next to the existing tests, per repo convention — no `packages/shared/tests/`): determinism, decode round-trip, alias-grammar match, no-uppercase/no-hyphen, min-length padding for small `n`, salt-affects-output, invalid/empty decode → `None`, `encode` rejects non-positive, empty-salt rejection (constructor + env factory via `monkeypatch`), and three retry-loop cases (advances on conflict and uses the *next* sequence value; gives up after **exactly** `max_attempts` calls; succeeds on the last allowed attempt). All async tests use injected callables — no DB.
  - **Verified:** `ruff check .` exit 0 ✓; `pytest` 16 passed (smoke + 3 migration tests against a real PG container + 12 shortcode unit tests) ✓; REPL spot-check with `HASHIDS_SALT=changeme` → `encode(1) == "e53ml3"` (6 chars, `[a-z0-9]`, `decode` → 1) ✓.

## Epic 4 — URL & alias validation + reserved words (shared) — COMPLETED
- **Intent:** Creation-time validation guards (scheme, length, SSRF, self-reference) and the reserved-word list, with the drift-prevention coverage test.
- **Scope:** `shared/validation.py` — URL parse + scheme ∈ {http,https}, length ≤ 2048, reject self-referential public host, DNS-resolve + reject private/loopback/link-local IPs (§5.5); alias normalize-to-lowercase + grammar `[a-z0-9-]` 3–32 no leading/trailing hyphen; reserved-word list (§5.4); helper exposing `proxy_served_paths` for the superset assertion.
- **Verification:** Unit tests for each reject/accept case (valid http(s), over-length, `javascript:`, private-IP host, self-host, good/bad aliases); reserved-word coverage test asserts `reserved_words ⊇ proxy_served_paths` (containment, not equality, per §5.10).
- **Depends on:** Epic 1.
- **Implementation notes (delivered):**
  - **Contract:** both validators **raise `ValidationError(reason, message)` on failure and return the normalized value on success** (`validate_alias` → lowercased alias, `validate_url` → the URL unchanged). The `reason` is one of the exported `REASON_*` string constants so Epic 6 can map to a 400 and branch/log symbolically without string-matching the message. Mirrors the module's existing exception style (`ShortCodeCollisionError`).
  - **DNS/SSRF is a synchronous injectable resolver** (`HostResolver = Callable[[str], list[str]]`). `validate_url(url, *, public_host=None, resolver=None)` stays pure-logic — no `asyncio` baked into the shared module. `default_host_resolver` wraps `socket.getaddrinfo` (dedupes addresses; `gaierror`/`OSError` → `[]` = unresolvable → reject). Epic 6 runs the validator off-thread; tests inject a fake resolver so private-IP rejection runs with **no real DNS**. (Chosen over an async signature per the planning decision.)
  - **SSRF predicate** rejects an address if `is_private or is_loopback or is_link_local or is_reserved or is_multicast or is_unspecified`, after **unwrapping IPv4-mapped IPv6** (`ip.ipv4_mapped`) so `::ffff:10.0.0.1` can't bypass it. Unparseable resolver output is rejected conservatively (treated as `REASON_URL_PRIVATE_ADDRESS`, never a 500). Rejection fires if **any** resolved address is disallowed (§5.5).
  - **URL checks run cheap→expensive:** length ≤ 2048 → `urlsplit` scheme ∈ {http,https} → host present (`parsed.hostname`, which lowercases + strips port/userinfo) → host ≠ public host (case-insensitive) → resolve → private-IP reject. Self-reference is checked before DNS (cheaper, clearer error).
  - **`PUBLIC_HOST` is a deliberate Epic-4 stand-in**, read from env via `default_public_host()` (raises `RuntimeError` if unset/empty) exactly like Epic 3's `default_short_code_generator()`; Epic 5's `config.py` will later route it through shared config. `validate_url` falls back to it only when `public_host` is not injected.
  - **`proxy_served_paths()` returns `{api, health, metrics, assets, static, dashboard, how-it-works}`** — the alias-collidable served segments only. **Dotted files (`favicon.ico`, `robots.txt`) and root `/` are excluded** (not extension-stripped): the grammar forbids `.` and requires ≥3 chars, so they can never be claimed as aliases and reserving them adds nothing (§5.4). Extension-stripping was rejected because it would invent reserved words (`favicon`, `robots`) that aren't real bare served paths. Docstring points forward to the Epic 18b Nginx work that must keep this set in sync.
  - **`RESERVED_WORDS` is a `frozenset`** (immutable, set-semantics for the `<=` drift test); `qr` and `admin` are the harmless over-reservation. The coverage test asserts `proxy_served_paths() <= RESERVED_WORDS` (subset/containment, not equality, §5.10), plus a test that no served path contains `.` and one that locks the helper's exact contents.
  - **Reserved-word vs. length precedence (discovered in test):** `qr` is only 2 chars, so `validate_alias("qr")` is rejected by the **length** guard before the reserved-word check ever runs — a sub-3-char reserved word can never be a valid alias regardless. The reserved-`reason` tests therefore parametrize only over reserved words ≥3 chars; a separate test asserts *every* reserved word is rejected for *some* reason.
  - **Exports** added to `linkshrink_shared.__init__`: `ValidationError`, `HostResolver`, `validate_alias`, `validate_url`, `proxy_served_paths`, `default_host_resolver`, `default_public_host`, `RESERVED_WORDS`, `ALIAS_MIN_LENGTH`/`ALIAS_MAX_LENGTH`/`MAX_URL_LENGTH`/`ALLOWED_URL_SCHEMES`, and all eleven `REASON_*` constants.
  - **Tests** in `tests/test_validation.py` (repo-root convention): alias accept/normalize/whitespace, length bounds (3/32 accept, 2/33 reject), grammar rejects (underscore/dot/space/non-ASCII), leading/trailing hyphen, parametrized reserved words (lower + upper); URL accept (http/https, mixed case), over-length, disallowed schemes + scheme-less, missing host, self-referential (case variant), parametrized private/loopback/link-local/IPv4-mapped addresses, any-address-private, unparseable + unresolvable resolver output, env-driven default public host; `default_host_resolver` via `monkeypatch` on `socket.getaddrinfo` (dedupe + `gaierror`→`[]`); `default_public_host` env read/raise; and the three anti-drift assertions. All unit-only, no Docker, no real DNS.
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` suite **88 passed, 3 skipped** (the 3 skips are the Docker-gated migration tests; run via the project `.venv`; the system Python 3.14 has neither the deps nor the editable `linkshrink_shared` install) ✓.

## Epic 5 — Config + Redis cache/queue helpers (shared) — COMPLETED
- **Intent:** Centralize env-driven config and the Redis key conventions (cache, negative cache, stream, rate-limit, metrics, heartbeat) so services agree on keys.
- **Scope:** `shared/config.py` (pydantic-settings, env-driven); `shared/cache.py` (cache-aside get/set helpers, `redirect:{code}` keys, negative sentinel `__404__`, TTL capping helper `min(86400, seconds_until_expires_at)`, metrics `INCR` helpers, **rate-limit key helpers** — `ratelimit:min:{ip}`/`ratelimit:day:{ip}` with fixed-window `INCR`-and-`EXPIRE`-on-first-hit per §5.9, owned here so the API in Epic 6 only consumes them); `shared/queue.py` (Streams `XADD`/`XREADGROUP`/`XACK`/`XAUTOCLAIM` wrappers, group/consumer naming, heartbeat key, **the authoritative click-event payload schema + (de)serializer** — `{link_id, ts, referrer, ua, source}` per §5.6 — so the redirect producer (Epic 11) and worker consumer (Epic 12) agree on one contract and can be built in parallel).
- **Verification:** Unit/integration tests (Testcontainers Redis): set→get round-trip, negative sentinel handling, TTL cap math, rate-limit key `INCR`/`EXPIRE` and over-limit detection, click payload serialize→deserialize round-trip, `XADD`→`XREADGROUP`→`XACK` cycle, metrics `INCR` reads back.
- **Depends on:** Epic 1.
- **Implementation notes (delivered):**
  - **`config.py`** — `Settings(BaseSettings)` with one field per `.env.example` var (case-insensitive mapping: `hashids_salt` ← `HASHIDS_SALT`, etc.). Every field has a default mirroring `.env.example` (secrets default to `""`) so `Settings()` **never raises on construction**; required-ness is enforced where consumed (the `default_*` helpers raise `RuntimeError` on empty). `database_url`/`redis_url` are `@property` accessors (the latter omits the `:password@` segment when no password). `get_settings()` is **deliberately uncached** (re-reads env each call, matching the direct `os.environ` reads it replaces); `get_redis_client()` builds an async `redis.asyncio.Redis` with `decode_responses=True`.
  - **Dev `.env`** — created a gitignored repo-root `.env` (copied from the committed `.env.example`) and configured `SettingsConfigDict(env_file=".env", extra="ignore")` so local runs load it. Process env vars still take precedence over the file; containers supply env via docker-compose. **Decision (refinement on plan):** kept `env_file` enabled (vs. the plan's earlier "no env_file") per user direction for a real shared dev env file.
  - **Stand-in refactor ("refactor now" decision):** `shortcode.default_short_code_generator` and `validation.default_public_host` now read `get_settings().hashids_salt`/`.public_host` (removing their direct `os.environ` reads) while keeping the **same `RuntimeError`-when-empty contract**; `migrations/env.py` falls back to `get_settings().database_url` (env.py's fallback host default thus shifts from `localhost` to config's `postgres`, correct for compose; the tests pin an explicit `sqlalchemy.url` so are unaffected). Imports use the package-absolute `from linkshrink_shared.config import get_settings` style (matching shortcode's existing import of `models`); no import cycle since `config.py` imports nothing from the package.
  - **`cache.py`** — async helpers over an injected client. Redirect cache: `redirect_key(code)` (**lowercases** the code so case variants share one entry; key ignores query string per §5.6), `cache_target`/`cache_negative`/`get_cached`/`is_negative`, `cap_positive_ttl = max(1, min(86400, n))`. Rate limit (§5.9): `hit_window` does `INCR` + **`EXPIRE … NX`** in one transaction pipeline (EXPIRE-NX = set-on-first-hit, atomic, no count==1 race); `check_rate_limit(ip) -> RateLimitResult(is_allowed, retry_after_seconds)` increments both windows and returns the exceeded window's TTL (day if both over). Metrics: `metrics:cache:hit`/`:miss`/`metrics:redirects:total` `INCR` helpers + `read_counter` (0 when missing).
  - **`queue.py`** — `ClickPayload` dataclass `{link_id, ts(datetime, tz-aware), referrer, ua, source(Source enum)}` with `serialize_click`/`deserialize_click` (flat `dict[str,str]`; `ts` via `isoformat`, `None` referrer/ua ↔ `""`). Thin async wrappers: `add_click` (`XADD … MAXLEN ~`), `ensure_consumer_group` (`MKSTREAM`, swallows `BUSYGROUP`), `read_clicks` (`XREADGROUP >`), `ack_click`, `claim_stale_clicks` (`XAUTOCLAIM`), `dead_letter`, `stream_length` (`XLEN`), `write_heartbeat`/`read_heartbeat`. Naming + caps (`clicks`/`analytics`/`clicks:dead`, `MAX_DELIVERY_ATTEMPTS=3`, `CLICKS_STREAM_MAXLEN=100_000`, `WORKER_HEARTBEAT_STALE_SECONDS=15`) are **module constants** (the "limits as constants" decision), not env-tunable. Heartbeat stored as epoch-seconds float string.
  - **Operational limits live as module constants** (per the confirmed decision): `RATE_LIMIT_PER_MINUTE=10`, `RATE_LIMIT_PER_DAY=100`, `POSITIVE_CACHE_MAX_TTL_SECONDS=86400`, `NEGATIVE_CACHE_TTL_SECONDS=60`, etc. — fixed §6 #13 design decisions, not pydantic settings.
  - **Tests** — `tests/test_config.py` (unit; isolates from `.env`/ambient env via `Settings(_env_file=None, **kwargs)`), `tests/test_cache.py` and `tests/test_queue.py` (pure-unit math/serialization + Testcontainers `redis:7-alpine` for the Redis-backed behaviors, reusing Epic 2's Docker-skip pattern; async client via `@pytest_asyncio.fixture` with per-test `flushdb`). The two **"empty salt/host → RuntimeError"** tests now use `monkeypatch.setenv(name, "")` (not `delenv`) so an env var overrides the dotenv source and the assertion holds whether or not a local `.env` exists.
  - **Exports** added to `linkshrink_shared.__init__`: `Settings`, `get_settings`, `get_redis_client`; all cache keys/constants/helpers + `RateLimitResult`; all queue naming constants + `ClickPayload`, the (de)serializers, and the stream/heartbeat wrappers.
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **117 passed** with Docker up (Redis integration + Epic 2 migration tests all run); on a Docker-less machine the 15 container-gated tests **skip cleanly** (pytest still exits 0), matching Epic 1/2. Redis Streams semantics (`XREADGROUP`/`XACK`/`XAUTOCLAIM` 3-tuple, timed-out read → falsy) confirmed against a real `redis:7-alpine` container.

## Epic 6 — API: create endpoint + rate limiting — COMPLETED
- **Intent:** The core `POST /api/links` flow — the first user-visible backend behavior.
- **Scope:** `services/api/` FastAPI app; `POST /api/links` (§5.8, §5.12): rate-limit check (fixed-window 10/min + 100/day, §5.9, trusting only the app-controlled real-IP header), URL/alias validation, alias-vs-generated branch (reserved/taken → 409, generated via shortcode with retry), `ttl_seconds` clamp [3600, 2592000] default 2592000, insert, `201` response `{ short_code, short_url, original_url, created_at, expires_at, qr_url }`; status codes 201/400/409/429 with `Retry-After`.
- **Verification:** Integration tests (Testcontainers PG+Redis): create returns 201 with correct shape; invalid URL → 400; reserved/taken alias → 409; exceeding minute or day window → 429 with `Retry-After`; `ttl_seconds` clamping at both bounds.
- **Depends on:** Epics 2, 3, 4, 5.
- **Note (minor):** the 201 response includes a `qr_url` string pointing at the QR endpoint that does not exist until Epic 9. This is only a constructed URL (no code dependency), but the returned link will not resolve until Epic 9 ships. Rate limiting consumes the rate-limit helper added to Epic 5.
- **Implementation notes (delivered):**
  - **New installable service package** `linkshrink_api` at `services/api/` using the same **src layout** as `packages/shared` (`services/api/src/linkshrink_api/`), with its own `pyproject.toml` declaring `fastapi`/`uvicorn[standard]` + a dependency on `linkshrink_shared` — the per-service-deps pattern Epic 1 deferred to each service. Added `httpx` to `requirements-dev.txt` (async test client) and `services/api/src` to the root `[tool.ruff] src` list so `linkshrink_api` sorts as first-party. Removed the now-stale `services/api/.gitkeep`.
  - **App factory + lifespan** (`main.py`): `create_app()` mounts the create router and exposes a module-level `app` for `uvicorn linkshrink_api.main:app`. A lifespan handler builds the long-lived async engine + `async_sessionmaker(expire_on_commit=False)` + Redis client onto `app.state` and disposes them on shutdown. Only `POST /api/links` ships here; `/health` and `/api/metrics` remain Epic 10 (not added early).
  - **Override-friendly dependencies** (`dependencies.py`): `get_db_session` (one session/request from `app.state.session_factory`), `get_redis`, `get_settings_dependency`, `get_host_resolver`, and `get_client_ip`. Tests override these via `app.dependency_overrides`, so the suite needs no lifespan and no real network.
  - **Trusted client IP = `X-Real-IP`** (constant `REAL_IP_HEADER` in `dependencies.py`), falling back to `request.client.host`, then `"unknown"`. The API never parses a raw `X-Forwarded-For` chain (§5.9); Nginx sets `X-Real-IP` in Epic 18b. **Decision (confirmed with user.)**
  - **Create flow** (`routers/links.py`) in the §5.12 order: `check_rate_limit` first (→ 429 + `Retry-After`, and a fixed-window hit is counted even when a later step fails) → `validate_url` run **off-thread** via `asyncio.to_thread` (its DNS/SSRF check is blocking) → alias-vs-generated branch → single `session.commit()`. `validate_alias` is pure and runs inline.
  - **Timestamps computed in-app** from one `datetime.now(UTC)`: `created_at` is set explicitly (overriding the model's `server_default`) and `expires_at = created_at + timedelta(seconds=clamp_ttl(...))`, so the two fields are consistent with no extra DB round-trip and no client clock-skew. **Decision (refinement on plan):** the model keeps its `server_default=now()` for other writers, but the API sets `created_at` itself.
  - **TTL clamp** (`schemas.py`): `clamp_ttl` clamps (never rejects) to `[TTL_MIN_SECONDS=3600, TTL_MAX_SECONDS=2592000]`, default `TTL_DEFAULT_SECONDS=2592000`. These bounds live in the API package as the create-endpoint's contract (may be promoted to shared config later if another service needs them).
  - **Persistence adapter** (`persistence.py`): `try_insert_link` inserts inside `session.begin_nested()` (savepoint) + `flush`, returning `False` **only** on the `uq_links_lower_short_code` violation and re-raising everything else — the exact `try_persist` contract from Epic 3. The generated path drives it through `generate_unique_short_code(get_next, try_persist, generator=...)` (where `get_next` wraps `fetch_next_sequence_value(session)` and `try_persist` wraps `try_insert_link`); the alias path calls `try_insert_link` once. `is_short_code_conflict` checks asyncpg's `constraint_name` and falls back to matching the index name in the error text, since a **functional** unique index does not reliably populate `constraint_name`.
  - **Short-code generator built from injected settings** (`_generator_for_salt(settings.hashids_salt)`, an `lru_cache`'d factory) rather than `default_short_code_generator()`, so it honors the test-provided `Settings` instead of re-reading the env — keeps the handler consistent with the injected config seam while reusing the (process-stable) hashids instance across requests instead of rebuilding it each create.
  - **Error mapping** (`errors.py`): `validate_url` failures → 400; `validate_alias` **reserved** → **409** (`REASON_ALIAS_RESERVED`) while length/grammar/hyphen → 400; **taken** alias (collision) → 409 (`alias_taken`); `ShortCodeCollisionError` → 500. All error bodies use `detail = {"reason", "message"}` so the Epic 15 frontend branches on a symbol, not message text. **Note:** reserved-alias is the one validation reason that maps to 409 not 400, matching the plan's "reserved/taken → 409".
  - **Response URLs** (`urls.py`): `short_url`/`qr_url` are built as `https://{public_host}/...` (canonical prod form; local/http deployments yield non-resolving strings the proxy serves later). **Decision (confirmed with user.)**
  - **Tests** (`tests/test_api_links.py`, repo-root convention) reuse the Epic 2/5 Docker-skip + Alembic-`upgrade head` + `async_sessionmaker` + Redis-`flushdb` patterns and drive the app via `httpx.AsyncClient` + `ASGITransport` with a fake public-IP resolver (a mutable `resolver_holder` lets the SSRF test swap in a private IP). 13 tests cover: 201 shape + `short_url`/`qr_url` format, custom-alias normalization, 400 (scheme/length/private-IP/bad-grammar alias), 409 (reserved + taken alias), TTL clamp at both bounds + default, and 429 with `Retry-After` for the minute window (loop 10 then the 11th) and the day window (seed `ratelimit:day:{ip}` to the cap, then one request).
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **130 passed** with Docker up (the 13 new API tests run against real PG + Redis containers); the container-gated tests skip cleanly without Docker, matching Epics 1/2/5. (Run via the project `.venv`.)

## Epic 7 — API: dashboard listing + link detail — COMPLETED
- **Intent:** Public read endpoints for the dashboard feed and single-link lookup.
- **Scope:** `GET /api/links?cursor=&limit=` keyset pagination on `(created_at DESC, id DESC)` with opaque base64 cursor, `limit` default 20 clamped to 100, response `{ items, next_cursor }` (§5.8); `GET /api/links/{code}` detail → 200/404.
- **Verification:** Integration tests: newest-first ordering, cursor paging across multiple pages is stable under inserts, `limit` clamp at 100, malformed cursor handled, unknown code → 404.
- **Depends on:** Epic 6.
- **Implementation notes (delivered):**
  - **Confirmed product decisions (with user):** (1) **one read view, `LinkView`,** is reused for both list items and detail — the `CreateLinkResponse` fields (`short_code, short_url, original_url, created_at, expires_at, qr_url`) **plus `is_custom`** so the dashboard can tell aliases from generated codes; (2) **expired links are shown** in both endpoints (no expiry filter) — they persist until the Epic 13 purge, and the redirect service still 404s expired codes on its own hot path; (3) a **malformed cursor → 400** `{reason: "invalid_cursor"}`, matching the existing structured-error style (not silently ignored).
  - **Schemas** (`schemas.py`): added `LinkView` and `ListLinksResponse {items, next_cursor}`, plus listing bounds `LIST_LIMIT_DEFAULT=20`/`LIST_LIMIT_MAX=100` and a `clamp_limit(None)→20` / `max(1, min(100, n))` helper mirroring the existing `clamp_ttl` (clamps, never rejects). The create endpoint keeps its own `CreateLinkResponse`.
  - **New `pagination.py` cursor module** (a focused concern like `urls.py`): `encode_cursor(created_at, id)` → urlsafe-base64 of `f"{created_at.isoformat()}|{id}"`; `decode_cursor` reverses it and **raises `ValueError`** on any malformed token (bad base64, missing separator, unparseable datetime/int via `datetime.fromisoformat`/`int`). The token is opaque-by-encoding only, not a security boundary.
  - **Endpoints** (`routers/links.py`): both `GET`s depend only on `get_db_session` + `get_settings_dependency` — **no redis/resolver/rate-limit** (reads are unthrottled, §5.9). A private `_link_to_view(link, public_host)` builds the view (reusing `build_short_url`/`build_qr_url`) so list and detail stay byte-identical.
  - **Keyset query:** `order_by(created_at DESC, id DESC)` with the cursor predicate `tuple_(Link.created_at, Link.id) < tuple_(cursor_created_at, cursor_id)` (SQLAlchemy `tuple_`), which maps cleanly onto the `ix_links_created_at_id_desc` index. **Next-page detection fetches `limit + 1` rows** (no `COUNT`): if the extra row comes back, `next_cursor` is built from the last in-page row and the page is trimmed; otherwise `next_cursor = None`. **Stable under inserts** because newer rows sort *above* any held cursor and never shift an in-progress page boundary.
  - **Detail lookup** is case-insensitive: `func.lower(Link.short_code) == code.lower()` (matches the `uq_links_lower_short_code` functional index and the redirect-path lookup); miss → `link_not_found_error` (404 `{reason: "not_found"}`).
  - **Errors** (`errors.py`): added `invalid_cursor_error` (400) and `link_not_found_error` (404), both via the existing `_detail(reason, message)` pattern.
  - **Tests** (added to `tests/test_api_links.py`, reusing its container/Alembic/`client` fixtures): rows are **seeded directly via the ORM `session_factory`** (a deliberate departure from Epic 6's API-driven inserts) — this sidesteps the 10/min create rate limit when >10 rows are needed and pins each `created_at` for deterministic ordering. 8 new tests cover newest-first + shape, default-20, limit-clamp-at-100, **cursor paging stable under a mid-paging insert** (no dupes/gaps, the new row never leaks), malformed cursor → 400, detail 200 shape incl. `is_custom`, case-insensitive detail, and unknown code → 404.
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **140 passed** with Docker up (the 8 new read tests run against real PG + Redis); container-gated tests skip cleanly without Docker, matching Epics 1/2/5/6. (Run via the project `.venv`.)

## Epic 8 — API: per-link analytics aggregation — COMPLETED
- **Intent:** Server-side aggregated analytics for a link.
- **Scope:** `GET /api/links/{code}/analytics` → 200/404 (§5.8): total clicks, daily buckets via `date_trunc('day', clicked_at)` at UTC (§7 decided), breakdowns by device_type / browser_family / os_family / referrer_domain / source.
- **Verification:** Integration tests: seed `click_events`, assert totals, UTC daily bucketing, and each breakdown grouping; unknown code → 404; empty link returns zeroed structure.
- **Depends on:** Epic 6 (reads `click_events` written later by the worker; tests seed rows directly).
- **Implementation notes (delivered):**
  - **New thin `analytics.py` query module** (`services/api/src/linkshrink_api/analytics.py`) holds all the aggregation SQL, next to `pagination.py`/`persistence.py`, so the router stays small (per CLAUDE.md). It exposes `aggregate_link_analytics(session, link_id) -> LinkAnalytics` (a frozen dataclass of raw `(value, count)` tuples) plus private `_count_total`/`_daily_buckets`/`_breakdown` helpers. The module deliberately returns plain tuples, not Pydantic — the router maps them to the wire models, keeping `analytics.py` schema-free like `pagination.py`.
  - **Endpoint** `GET /api/links/{code}/analytics` in `routers/links.py` (`get_link_analytics`): injects **only** `session` (no settings/redis/resolver — the response carries no URLs and reads are unthrottled per §5.9). It reuses the detail endpoint's exact case-insensitive lookup `select(Link).where(func.lower(Link.short_code) == code.lower())` and the existing `link_not_found_error(code)` for 404 — **no new error factory was needed**. A small `_to_breakdown` helper maps tuple rows to `BreakdownItem`s.
  - **Response schemas** (`schemas.py`): `LinkAnalyticsResponse {short_code, total_clicks, daily, by_device_type, by_browser_family, by_os_family, by_referrer_domain, by_source}`, with `DailyClickBucket {day: date, count: int}` and `BreakdownItem {value: str, count: int}`. Added `date` to the `datetime` import. A zero-click link returns naturally — `total_clicks=0`, every list `[]` — with no special-casing.
  - **Confirmed product decisions (with user):** (1) **NULL breakdown values coalesce to the literal `"unknown"`** for the three nullable text dimensions (`referrer_domain`/`browser_family`/`os_family`) via `func.coalesce(column, "unknown")`, so each breakdown's counts always sum to `total_clicks`; the two NOT-NULL enum columns (`device_type`/`source`) skip coalesce. (2) **The daily series is sparse** — only UTC days that actually have clicks, oldest first (the frontend fills any gaps).
  - **UTC daily bucketing** (§7 decided) uses `func.date_trunc("day", ClickEvent.clicked_at.op("AT TIME ZONE")("UTC"))` — the `AT TIME ZONE 'UTC'` converts the `timestamptz` to a UTC wall-clock timestamp before truncation, so the day floor is a real UTC calendar day **independent of the DB session timezone**. The grouped value comes back as a naive timestamp at UTC midnight; the module calls `.date()` on it to emit a `date`.
  - **Breakdown ordering** is `func.count().desc(), cast(value, String).asc()` — most-clicks-first, with a value-ascending tiebreak making ties deterministic (covered by a dedicated test). The tiebreak value is **cast to text** (`cast(value, String)`) so enum dimensions (`device_type`/`source`) sort alphabetically by their string value rather than by the PostgreSQL enum type's declaration order — a raw `enum ASC` would order by declaration. Enum-column group values arrive back as `StrEnum` members; `str(value)` in the router mapping yields their string value (`"desktop"`, `"qr"`, …).
  - **Query strategy:** seven separate aggregates run **sequentially on the one request session** (no parallel sessions/`gather`), each backed by the existing `ix_click_events_link_id_clicked_at` index. Clarity over micro-optimization for a portfolio read path.
  - **Tests** (added to `tests/test_api_links.py`, reusing its container/Alembic/`client`/`session_factory` fixtures): a `_seed_clicks` ORM helper plus a `_click(**overrides)` default-builder seed `ClickEvent` rows directly (the worker doesn't exist yet). 7 tests cover unknown code → 404, empty link → zeroed structure, total count, the **load-bearing UTC-midnight-straddle** test (clicks at 23:30 and 00:30 land in two separate daily buckets), enum breakdowns (device_type/source) with most-clicks-first ordering, NULL→`"unknown"` coalescing across all three text dimensions, and the deterministic value-ascending tiebreak. Imports `ClickEvent`/`DeviceType`/`Source` (already exported from `linkshrink_shared`) and `select`.
  - **Verified:** `ruff check .` exit 0 ✓ (one import-sort auto-fixed); full `pytest` **148 passed** with Docker up (the 7 new analytics tests run against real PG + Redis); container-gated tests skip cleanly without Docker, matching Epics 2/5/6/7. (Run via the project `.venv`.)

## Epic 9 — API: QR generation — COMPLETED
- **Intent:** On-demand QR images for short links.
- **Scope:** `GET /api/links/{code}/qr?format=png|svg` → 200/404 (§5.8): PNG default 512px ECC=M, SVG option; not stored; encodes the short URL with `?source=qr` attribution.
- **Verification:** Integration tests: PNG returns `image/png` ~512px, SVG returns `image/svg+xml`, encoded payload includes `?source=qr`, unknown code → 404.
- **Depends on:** Epic 6.
- **Implementation notes (delivered):**
  - **QR library is `segno`** (`segno>=1.6`, added to `services/api/pyproject.toml` — an API-only runtime dep, not shared). Chosen over `qrcode[pil]` because it is **pure-Python (no Pillow)** and one library renders both PNG and SVG with ECC=M. **Decision (confirmed with user.)**
  - **New `qr.py` rendering module** (`services/api/src/linkshrink_api/qr.py`), a focused unit like `pagination.py`/`urls.py` so the router stays small (per CLAUDE.md). It exposes `render_qr(payload, image_format) -> (bytes, media_type)` plus the module constants `QR_TARGET_SIZE_PX=512`, `QR_ERROR_CORRECTION="m"` (ECC=M, the §6 decision), `QR_BORDER=4` (standard quiet zone), and `QR_MEDIA_TYPES={"png": "image/png", "svg": "image/svg+xml"}`.
  - **~512px via computed scale:** `scale = max(1, round(512 / code.symbol_size(scale=1, border=4)[0]))`, passed to `segno`'s `save(..., scale=scale, border=4)` for both PNG and SVG. The AC says "~512px" (not exact) — scale is integer-quantized to keep modules crisp (no interpolation), so a typical short-URL QR lands within ~±50px of 512. Rendering is **synchronous/inline** in the handler (sub-millisecond CPU; not worth `asyncio.to_thread` unlike create's blocking DNS check).
  - **`?source=qr` attribution** is built by `build_qr_payload(public_host, short_code)` in `urls.py` — `build_short_url(...) + "?source=qr"`, reusing the existing short-URL builder. The redirect service (Epic 11) reads this query param for the click `source` and it is **not** part of the redirect cache key (§5.6), so QR and direct hits share one cache entry. The endpoint encodes this payload (not the bare short URL).
  - **Endpoint** `GET /api/links/{code}/qr` in `routers/links.py` injects **only** `session` + `settings` (no redis/rate-limit — reads are unthrottled, §5.9). The `format` query param is bound as `image_format: Annotated[str, Query(alias="format")] = "png"` — the wire key stays `?format=` while the variable avoids shadowing the `format` builtin (naming conventions). It is lowercased, then anything outside `{png, svg}` raises `invalid_qr_format_error` (**400** `{reason: "invalid_format"}`, a new factory in `errors.py` mirroring `invalid_cursor_error`). Returns a raw `fastapi.Response(content=..., media_type=...)` — no Pydantic schema, **no `Content-Disposition`** (left inline so the frontend can both display and download it).
  - **Lookup reuses the detail endpoint's exact case-insensitive query** `select(Link).where(func.lower(Link.short_code) == code.lower())` and the existing `link_not_found_error(code)` for 404 — no new lookup helper needed. **Expired links are still served** (consistent with the Epic 7/8 detail/analytics reads); only an unknown code is a 404. The redirect hot path owns the expiry-404 (Epic 11), not QR. **Decision (clarified with user)** — no changes to Epics 7/8.
  - **HTTP caching (`Cache-Control: public, max-age=86400, immutable`)** — added in review. A QR for a given code+format is immutable (it only encodes the short URL, which never changes), so the response is safe for clients/proxies/CDN to keep. The header constants (`QR_CACHE_MAX_AGE_SECONDS=86400`, `QR_CACHE_CONTROL`) live in `qr.py`; the server still **never stores** the image (re-renders only when a request actually reaches it) — the header just lets *downstream* holders reuse it, which is orthogonal to the "not stored" requirement. The main win is the browser cache (a user re-viewing the same QR sends no request); shared caching is realized in Epic 18b. **Decision (confirmed with user):** at scale this is fronted by a CDN (QR images are the textbook edge-cache candidate — immutable, deterministic per code+format, no per-user variation); on-demand render + edge cache beats precomputing every QR to object storage since most links are never scanned. **Caveat carried to 18b:** the proxy/CDN cache key must **vary on `?format`** (png vs svg are different images at the same path); `?source=qr` is inside the encoded payload, not the request URL, so it never affects the key.
  - **Tests** (6 added to `tests/test_api_links.py`, reusing its container/Alembic/`client`/`_seed_links` fixtures): PNG default → `image/png` with the PNG magic bytes and IHDR width within ±100px of 512 (parsed via `struct.unpack(">I", body[16:20])` — **no Pillow**); `?format=svg` → `image/svg+xml` with `<svg` in the body; case-insensitive code; unknown code → 404; `?format=gif` → 400 `{reason: "invalid_format"}`. The **`?source=qr` AC is verified at the construction seam** — a unit test asserts `build_qr_payload(...) == https://{host}/{code}?source=qr` — because `segno` is encode-only (no pure-Python decoder exists to read the payload back out of the pixels); the endpoint provably feeds that exact string to the renderer.
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **155 passed** with Docker up (the 6 new QR tests run against the real PG container + 1 pure-unit payload test); container-gated tests skip cleanly without Docker, matching Epics 2/5/6/7/8. (Run via the project `.venv`.)

## Epic 10 — API: metrics + health — COMPLETED
- **Intent:** Live operational metrics for the UI and an HTTP health check.
- **Scope:** `GET /api/metrics` → JSON derived live numbers (cache hit ratio from `metrics:cache:hit`/`:miss`, queue depth via `XLEN`/PEL, total redirects / throughput) per §5.7; `GET /health` → 200.
- **Verification:** Integration tests: seed Redis counters/stream, assert computed hit ratio and queue depth; `/health` returns 200.
- **Depends on:** Epics 5, 6.
- **Note (minor):** the `metrics:cache:hit`/`:miss`/`metrics:redirects:total` counters are written by the redirect service (Epic 11). The dependency list (5, 6) is correct for building and testing this endpoint (tests seed the counters directly, same pattern as Epic 8), but the live numbers are only meaningful once Epic 11 is producing traffic.
- **Implementation notes (delivered):**
  - **Response shape** (flat JSON, Epic 20 will "finalize"): `GET /api/metrics` → `{cache_hits, cache_misses, cache_hit_ratio, total_redirects, queue_pending, queue_stream_length, worker_healthy, worker_heartbeat_age_seconds}`; `GET /health` → `{"status": "ok"}`. Flat (not nested) so the Epic 17 frontend reads numbers without unwrapping; new Pydantic `MetricsResponse`/`HealthResponse` in `schemas.py`.
  - **Confirmed decisions (with user):** (1) **Two queue numbers, clearly named** — `queue_pending` is the **real backlog** (`XPENDING` summary = delivered-but-unacked PEL size) and `queue_stream_length` is the **XLEN volume gauge** (total recent entries, capped at `MAXLEN ~100_000`, and **does not drop on `XACK`** so it is not backlog). The original plan's single `queue_depth`/`XLEN` was renamed/split after clarifying that XLEN alone is misleading as "depth". (2) **Worker liveness is included** (beyond §5.7's four numbers) — `worker_healthy` + `worker_heartbeat_age_seconds`, derived from the existing `metrics:worker:heartbeat` key, a natural live operational signal for the Epic 17 UI. (3) **`/health` is pure liveness** — always `200`, **no DB/Redis checks**, so the Epic 18a container healthcheck can't flap on a slow dependency.
  - **New `pending_count` helper in `shared/queue.py`** (`pending_count(redis, *, group, stream) -> int`), exported from `linkshrink_shared`, sitting next to `stream_length`. It calls `redis.xpending(stream, group)` (summary form) and returns the `pending` field; it **swallows the `NOGROUP` `ResponseError` → `0`** so the endpoint works before the worker has ever created the `analytics` group (and re-raises any other error rather than masking it). `stream_length`'s docstring was clarified to say it is a volume gauge, not backlog.
  - **New thin `metrics.py` collector module** (`services/api/src/linkshrink_api/metrics.py`), next to `analytics.py`/`pagination.py` so the router stays small (per CLAUDE.md). It exposes a frozen `MetricsSnapshot` dataclass + `async def collect_metrics(redis, *, now=None) -> MetricsSnapshot`; the router maps the snapshot to the wire model by name via `MetricsResponse(**asdict(snapshot))` (snapshot and schema share field names one-for-one, so a new metric is added in the dataclass + schema only, not in a hand-written mapping). It reuses the shared helpers `read_counter` (cache hit/miss + redirects), `stream_length`, `pending_count`, and `read_heartbeat` — no new Redis access patterns.
  - **Derivations:** `cache_hit_ratio = hits / (hits + misses)` with a **zero-denominator guard → `0.0`**, rounded to 4 dp (`HIT_RATIO_PRECISION`) for a clean payload. Worker health: `now` defaults to `time.time()` (**injectable** so tests pin the heartbeat age deterministically); `age = max(0.0, now - float(heartbeat))` (the `max(0.0, …)` clamp keeps the exposed age non-negative under API/worker clock skew — the worker writes the heartbeat, the API reads it); `worker_healthy = heartbeat is not None and age <= WORKER_HEARTBEAT_STALE_SECONDS` (15s, the §5.2 constant); age is `None` (and healthy `False`) when no heartbeat exists.
  - **Router** `routers/system.py` (new) holds both `/health` (no deps) and `/api/metrics` (depends only on `get_redis` — no settings/DB, the response carries no URLs and reads are unthrottled per §5.9), mounted in `main.py` via `app.include_router(system.router)`. No new error factories — both endpoints always `200`.
  - **Tests:** `tests/test_queue.py` — 2 added (Redis container): `pending_count` returns `0` when the group is missing (NOGROUP swallowed), and reports the delivered-unacked count after `ensure_consumer_group` + `add_click` + `read_clicks`, dropping by one after an `ack_click`. `tests/test_api_links.py` — 6 added (reusing the `client`/`redis_client` fixtures; metrics needs only Redis): `/health` → `{"status":"ok"}`; empty state → all-zeros + ratio `0.0` + `worker_healthy=False` + age `None` + exact field-set lock; seeded counters → ratio `0.8` + total redirects; seeded stream+group → `queue_pending`/`queue_stream_length` both 2; recent heartbeat → healthy; stale heartbeat → unhealthy. Renamed the new payload helper to `_click_payload` to avoid colliding with Epic 8's existing `_click(**overrides)` analytics builder.
  - **Post-review cleanups:** (1) the router mapping was collapsed to `MetricsResponse(**asdict(snapshot))` (see collector note above) to remove the hand-written field-by-field copy and its drift risk; (2) the heartbeat age gained the `max(0.0, …)` clock-skew clamp (see Derivations). Both are refactors covered by the existing tests — no test changes. The reviewer's third suggestion (pipelining the ~6 Redis reads) was **declined**: it would force bypassing the shared helpers and reimplementing the `pending_count` NOGROUP-swallow against `pipeline.execute()`'s per-command error semantics, trading the clean helper reuse for negligible round-trip savings on a low-frequency endpoint. `/api/metrics` staying unauthenticated is the documented §5.9 decision (no URLs in the payload, reads unthrottled).
  - **Verified:** `ruff check .` exit 0 ✓ (one import-sort auto-fixed); full `pytest` **163 passed** with Docker up (the 8 new tests run against real PG + Redis); container-gated tests skip cleanly without Docker, matching Epics 2/5/6/7/8/9. (Run via the project `.venv`.)

## Epic 11 — Redirect service — COMPLETED
- **Intent:** The minimal hot-path redirect with cache-aside, negative caching, and best-effort click emission.
- **Scope:** `services/redirect/` FastAPI app; `GET /{code}` (§5.6): Redis HIT → 302 (no expiry check, correctness via capped TTL); NEG-HIT → 404; MISS → DB lookup `lower(short_code)`, positive cache with `EX min(86400, seconds_until_expires_at)` or negative `__404__` EX 60; `XADD` click payload (best-effort, swallow+log failures, never blocks 302); cache key ignores query string; `?source=qr`→source field; metrics `INCR` counters; `/health`.
- **Verification:** Integration tests: known code → 302 to target + click queued; cache hit serves without DB; **expired code does not 302 even on a warm path** (explicit AC, §5.6); unknown → 404 with negative cache; analytics failure still returns 302.
- **Depends on:** Epics 2, 5. (The redirect path is a pure string lookup `lower(short_code)=:code` — it never decodes hashids — so the short-code generator from Epic 3 is not needed. The click payload it `XADD`s uses the shared schema from Epic 5.)
- **Implementation notes:**
  - **Cache value now carries `link_id`.** A cache HIT must `XADD` a click, and `ClickPayload` requires `link_id`, but the Epic 5 cache stored only the URL. Changed the shared `cache_target` to store JSON `{"id": link_id, "url": original_url}` and added `CachedTarget` + `decode_cached_target` to `linkshrink_shared.cache` (exported from the package). The negative sentinel stays a plain `__404__` string so `is_negative` is unchanged. The Epic 5 `tests/test_cache.py` was updated to the new value shape (decision confirmed with user).
  - **A negative-cache hit counts as a cache hit** (`metrics:cache:hit`) — it served from cache without touching Postgres, the natural hit-ratio semantics. `metrics:cache:miss` is bumped only on a true Redis miss; `metrics:redirects:total` only on an actual 302 (in the router, not the resolver).
  - **302 carries `Cache-Control: no-store`** so the redirect stays deliberately non-cacheable (§5.6).
  - **Best-effort side effects:** `add_click` and `increment_redirects_total` share one swallow-and-log `try/except` in the router, so neither a queue nor a metrics hiccup can break the 302.
  - **Structure mirrors `services/api`:** `main.py` (lifespan + `create_app`), `dependencies.py` (`get_settings_dependency`/`get_db_session`/`get_redis` — no client-IP/SSRF providers; redirects are unthrottled and never re-resolve DNS), `resolution.py` (the testable cache→DB→cache `resolve_code`), `routers/system.py` (`/health`, inline `HealthResponse`), `routers/redirect.py` (`GET /{code}`). The system router is mounted **before** the `/{code}` catch-all so `/health` is never shadowed; `{code}` is a single-segment param, so it cannot swallow slashes.
  - Root `pyproject.toml` ruff `src` and the README editable-install list now include `services/redirect`.
  - **Post-review cleanups:** (1) `resolve_code` now returns the shared `CachedTarget` directly instead of a duplicate `ResolvedLink` dataclass (same two fields); (2) a malformed positive cache entry is decoded via `_decode_or_none` and, on failure, treated as a miss so the hot path re-resolves from Postgres and rewrites the entry — a corrupt/stale-shape value can never 500 the redirect (new `test_malformed_cache_entry_is_treated_as_miss_and_self_heals`); (3) the resolver's `cache:hit`/`cache:miss` `INCR`s are now best-effort (swallowed+logged via `_count`), matching the router's posture so a metrics hiccup never breaks resolution; (4) removed the now-redundant `services/redirect/.gitkeep` placeholder (the directory holds source).

## Epic 12 — Worker: analytics consumer — COMPLETED
- **Intent:** Durable at-least-once consumption that derives PII-free fields and survives crashes.
- **Scope:** `services/worker/` asyncio consumer group `analytics` (§5.7): `XREADGROUP` loop, UA parse → device/browser/OS, Referer → host only, insert `ClickEvent`, `XACK`; `XAUTOCLAIM` recovery of idle entries; DLQ to `clicks:dead` + `XACK` after 3 attempts; `MAXLEN ~` trim; heartbeat `metrics:worker:heartbeat` each loop with 15 s health threshold (§5.2); healthcheck script.
- **Verification:** Integration tests (Testcontainers): `XADD` → row appears with derived coarse fields and no raw UA/IP; kill/restart mid-stream → pending entries reclaimed; poison message dead-lettered after 3 tries; heartbeat key updates.
- **Depends on:** Epics 2, 5. The worker consumes the **click-event payload schema defined in Epic 5** (`shared/queue.py`), not the redirect service itself — its tests `XADD` directly — so it can be built in parallel with Epic 11. Epic 11 is only a runtime/integration prerequisite (it is the real producer of the stream), exercised together in Epic 19.
- **Implementation notes:**
  - **UA parsing library: `user-agents`** (added to `services/worker/pyproject.toml`; pulls in `ua-parser`). Its `is_tablet`/`is_mobile`/`is_pc` flags map onto `DeviceType` (tablet checked before mobile, since a tablet also reports `is_mobile`), falling back to `unknown` for bots/unparseable agents. Browser/OS families come from `parsed.browser.family`/`parsed.os.family`; the library's `"Other"` sentinel (and any blank) is stored as `None` so the Epic 8 breakdowns coalesce it to "unknown". Referrer host uses stdlib `urllib.parse.urlparse(...).hostname`, lowercased — no new dependency. Both derivations are pure functions in `parsing.py`, unit-tested without Docker.
  - **Not a FastAPI app.** The worker is a plain asyncio process (`python -m linkshrink_worker.main`) that builds its own engine/session-factory/redis directly, rather than holding them on `app.state` like api/redirect. `main.py` ensures the consumer group then runs two `asyncio` tasks — the consumer loop as the main task and a periodic recovery task — wired for SIGINT/SIGTERM graceful shutdown (with a Windows fallback to `signal.signal` since `add_signal_handler` is unimplemented there; the fallback hops back onto the loop via `loop.call_soon_threadsafe(stop.set)` since `Event.set` is not thread-safe). The recovery interval is a cancellable `wait_for(stop.wait())` (shared `_sleep_or_stop` helper) so shutdown is immediate. Each loop **wraps its pass in try/except**: a transient failure (e.g. a Redis disconnect) is logged and retried — the consumer after a short `ERROR_BACKOFF_SECONDS` backoff, recovery on its next interval — rather than killing the loop task and leaving the worker alive-but-idle until the stale heartbeat trips a restart. The Epic 13 purge job slots in via the same `create_task` + interval pattern.
  - **Single-pass coroutines, not buried loops.** `consumer.run_consumer_once` and `run_recovery_once` each do exactly one pass and are what the integration tests drive directly; the `main.py` loops just call them in a cycle. The heartbeat (`time.time()` epoch seconds via the shared `write_heartbeat`) is written at the **top of every consumer pass**, even when the read returns nothing, so an idle worker (blocking ≤5 s) stays well inside the 15 s threshold.
  - **At-least-once via the pending list.** A failed entry is never `XACK`ed, so it stays in the PEL and is retried by the next recovery pass. Recovery reclaims idle entries with `XAUTOCLAIM` (the shared `claim_stale_clicks`, paging until the cursor wraps to `0-0`), looks up each entry's delivery count via `redis.xpending_range`, and **dead-letters + acks** any entry whose count has reached `MAX_DELIVERY_ATTEMPTS` (3) — so a poison message is dead-lettered on its third delivery (initial read = 1, two reclaims = 2 and 3). A click for a nonexistent `link_id` is the deterministic poison used in tests (FK violation). All stream ops reuse the shared `queue.py` wrappers; the only direct Redis call is the `xpending_range` delivery-count lookup (worker-specific orchestration). That lookup is **scoped to the reclaiming consumer and the claimed id range** (`min=first..max=last` of the just-claimed ids, `consumername=<recovery consumer>`), not a global `-`/`+` window — otherwise a backlog or another consumer's not-yet-idle pending entries could crowd a claimed entry out of the result, default its count to 1, and retry a poison message forever instead of dead-lettering it at the cap.
  - **Healthcheck script** (`python -m linkshrink_worker.healthcheck`) reads the heartbeat and exits `1` if missing/unparseable or older than `WORKER_HEARTBEAT_STALE_SECONDS`, else `0`; the Docker wiring lands in Epic 18a. Worker-only tunables (batch count, block ms, recovery idle/interval, `WORKER_NUMBER`) live in `config.py`; the shared contract constants stay in `linkshrink_shared.queue`.
  - Root `pyproject.toml` ruff `src` now includes `services/worker/src`; the worker is installed editable (`pip install -e services/worker --no-deps`, since `linkshrink_shared` is a local editable, then `pip install "user-agents>=2.2"`). The README "Development setup" editable-install list also adds `pip install -e services/worker` so a fresh clone can import `linkshrink_worker`/`user-agents` and the worker tests run (rather than failing at import).
  - **Post-review cleanups:** (1) the recovery delivery-count lookup is now scoped to the reclaiming consumer + claimed id range instead of a global `-`/`+` `xpending_range` window — a backlog could otherwise hide a claimed entry's true attempt count and prevent dead-lettering (see the at-least-once note above); (2) both loops now catch/log/retry transient errors (consumer backs off `ERROR_BACKOFF_SECONDS`, recovery retries next interval) via the shared `_sleep_or_stop` helper, so a Redis blip self-heals; (3) the Windows signal fallback uses `loop.call_soon_threadsafe(stop.set)` for thread-safe shutdown; (4) README editable-install list now includes the worker.
  - **Verified:** `ruff check services/worker/src tests/test_worker.py` exit 0 ✓; worker suite **18 passed** with Docker up (4 container-backed integration tests against real PG + Redis, including the dead-letter path that exercises the corrected delivery-count lookup); container-gated tests skip cleanly without Docker, matching Epics 2/5/6/7/8/9/10/11. (Run via the project `.venv`.)

## Epic 13 — Worker: scheduled purge job — COMPLETED
- **Intent:** Permanent deletion 3 months after expiry, on a background interval.
- **Scope:** `asyncio` background task on a fixed interval (§5.2, §5.12): `DELETE FROM links WHERE expires_at < now() - interval '3 months'` (cascades to `click_events`); runs alongside the consumer loop.
- **Verification:** Integration test: seed an expired-3-months link with clicks → purge deletes both link and cascaded events; a recently-expired link is retained (still 404s via redirect but row stays).
- **Depends on:** Epic 12.
- **Note (recommended — resolves a TDD inconsistency):** §9.6 lists "neg-cache cleanup" as worker scope, but §5.2 establishes that **no cache-cleanup job is needed** — the positive cache TTL is capped at `min(86400, seconds_until_expires_at)` so a `302` entry can never outlive its link, and negative entries self-expire at 60 s. The TTL caps are sufficient; this epic is deliberately purge-only. Do not add a sweep job back in (a reviewer should not treat its absence as an omission).
- **Implementation notes:**
  - **Single-pass coroutine, mirroring the consumer.** New `services/worker/src/linkshrink_worker/purge.py` exposes `run_purge_once(session_factory)` — one pass that the `main` loop calls on an interval and the integration test drives directly (same shape as `run_consumer_once`/`run_recovery_once`). Returns the number of links deleted; logs at INFO only when it deleted something.
  - **One bulk delete, DB does the rest.** `delete(Link).where(Link.expires_at < func.now() - cast(retention_interval, INTERVAL)).execution_options(synchronize_session=False)` — the cutoff uses Postgres' own clock (`now()`, matching the redirect service's expiry check), and the `click_events → links` `ON DELETE CASCADE` FK removes the cascaded events. No per-row ORM loading and no separate delete of click events. The retention string is `cast(..., INTERVAL)` so it binds as a parameter (no string interpolation, injection-proof even if a future caller passes an untrusted value), and `synchronize_session=False` skips the otherwise-redundant `RETURNING` of every deleted PK on a fire-and-forget bulk purge.
  - **Config knobs** in `config.py`: `PURGE_RETENTION_INTERVAL = "3 months"` and `PURGE_INTERVAL_SECONDS = 86_400` (daily — the right cadence for a 3-month window; a missed pass just defers a row's deletion to the next day). `run_purge_once` takes the retention as a defaulted keyword arg for test readability.
  - **Third asyncio task.** `main.run()` now creates a `_purge_loop(session_factory, stop)` alongside the consumer and recovery loops — same `while not stop.is_set()` / try-except-log / cancellable `_sleep_or_stop(stop, PURGE_INTERVAL_SECONDS)` pattern as `_recovery_loop`. The purge loop needs only the session factory (no Redis); the existing `finally` block cancels/awaits it on shutdown.
  - **No schema change.** The `ix_links_expires_at` index and the `ON DELETE CASCADE` FK were already added in the initial migration (Epic 2) specifically for this job.
  - **Verified:** `ruff check services/worker/src tests/test_worker.py` exit 0 ✓; worker suite **19 passed** with Docker up (the new boundary test seeds three links — expired 100, 85, and 1 days ago, each with a click → purge deletes only the 100-day link and its cascaded click event and returns 1, while the 85-day link just inside the ~89-day window and the 1-day link both keep their row and click). Container-gated tests skip cleanly without Docker.

## Epic 14 — Frontend: scaffold, theme, layout, API client, demo banner — COMPLETED
- **Intent:** Vite React SPA foundation with the design system, routing shell, and the required public-demo warning.
- **Scope:** `frontend/` Vite + React; dark-only theme per UI/UX guide (palette, Inter, 1200px max width, card layout, mobile single-column + collapsible nav); app shell + client-side routes (`/`, `/dashboard`, `/how-it-works`); typed API client for `/api/*`; persistent public-demo warning banner + landing-page "do not shorten private/sensitive links" notice (§5.10, hard AC); CLAUDE.md compliance (unique `id` on every element, small components, descriptive naming, boolean prefixes).
- **Verification:** `npm run build` succeeds; app renders shell, nav, and the demo/visibility warning; routes navigate client-side; manual check that every rendered element carries a unique `id`.
- **Depends on:** Epic 1.
- **Note (minor — sizing):** this is one of the heavier epics (scaffold + theme + layout + API client + banner). It is cohesive enough to ship as one commit, but if it grows during implementation it is a fair candidate to split (e.g. scaffold+theme+layout vs. typed API client + demo/visibility warning).
- **Implementation notes (delivered):**
  - **Stack:** Vite 5 + React 18 + **TypeScript** (the "typed API client" scope makes TS the natural choice). `react-router-dom` v6 for client-side routing; `@fontsource/inter` self-hosts the Inter font so there is no external font-CDN dependency. Lives entirely under `frontend/` (the Epic 1 `.gitkeep` placeholder is replaced).
  - **Styling = CSS Modules + design tokens** (confirmed with user, over Tailwind/plain-global). `src/styles/tokens.css` holds the whole palette/spacing/radius/layout scale as CSS custom properties (single source of truth); `src/styles/global.css` is the reset + base typography; every component has its own `*.module.css` referencing the tokens. Tokens are imported before global in `main.tsx` so the variables exist when the base styles read them.
  - **Data layer = plain typed `fetch` wrapper** (confirmed with user, no TanStack Query yet). `src/api/types.ts` mirrors `schemas.py` (datetimes/dates are ISO strings over JSON); `src/api/client.ts` exposes one function per endpoint (`createLink`, `listLinks`, `getLink`, `getLinkAnalytics`, `getMetrics`) plus a `buildQrUrl` helper (QR is an image URL, not a JSON call). Pages do **not** call the client yet — that lands in Epics 15/16; the scaffold only provides it.
  - **Error envelope:** confirmed against `errors.py` that FastAPI wraps the structured error under `detail` — bodies are `{"detail": {"reason", "message"}}`. The client parses `body.detail.*` and throws an `ApiError` carrying `status` + `reason` + `message` so callers branch on the symbolic reason, not message strings.
  - **Relative paths + dev proxy:** the client only ever uses relative `/api/...` paths. In production Nginx (Epic 18b) serves the SPA and `/api/*` same-origin; in dev `vite.config.ts` proxies `/api` → `http://localhost:8000` to stand in for Nginx. No base-URL env var needed.
  - **Demo/visibility warning split into two components** (covers both halves of the §5.10 hard AC): `DemoBanner` renders at the top of the shell on **every** route (persistent), and `VisibilityNotice` renders on the **Home page** spelling out the full "do not shorten private, sensitive, or confidential links" warning. The footer carries a short standing reminder too.
  - **Layout shell** (`AppShell` = banner + header + routed `main` + footer; the `Outlet` keeps the shell mounted across navigation). Header/nav are small components: `SiteHeader` owns the mobile open/closed state and shares it with `MobileNavToggle` (hamburger, `aria-controls`/`aria-expanded`) and `SiteNav` (collapses < 640px). Nav links come from a single `src/navigation.ts` array so Epics 15–17 extend one list; its paths (`dashboard`, `how-it-works`) already sit in the server's `RESERVED_WORDS`.
  - **Pages are placeholders** per scope: `HomePage` renders the hero with a **disabled** `ShortenPlaceholder` input (the real shorten flow is Epic 15) plus the visibility notice; `DashboardPage`/`HowItWorksPage` reuse a shared `PagePlaceholder` card naming the epic that fills them (16/17).
  - **CLAUDE.md compliance:** every rendered element carries a unique, descriptive `id` (including `index.html` head elements, wrappers, headings, list items, icons); components are small and single-responsibility; names are descriptive with boolean `is*` prefixes (`isMobileNavOpen`, `isOpen`, `is_custom` mirrored as `is_custom` in types).
  - **Verified:** `npm install` ✓ (180 packages); `npm run build` (`tsc -b && vite build`) ✓ succeeds; `npm run lint` (ESLint flat config) ✓ clean; `npm run dev` boots and serves `/`, `/dashboard`, `/how-it-works` client-side (200 via SPA fallback) with the demo banner + visibility notice rendering.

## Epic 15 — Frontend: Home (shorten → result card) — COMPLETED
- **Intent:** The primary paste → shorten → copy → QR flow.
- **Scope:** Home page URL input as primary focus; submit → `POST /api/links`; result card with short URL, copy button (green success), QR download (PNG/SVG via `/qr`); inline validation/error states (400/409/429 surfaced clearly); small focused components per CLAUDE.md.
- **Verification:** Manual: paste a URL → get short link, copy works, QR downloads; error cases (bad URL, taken alias, rate limited) show clear messages. Against the running API stack.
- **Depends on:** Epics 6, 9, 14.
- **Implementation notes (delivered):**
  - **Replaced the Epic 14 placeholder with a live `ShortenForm`** (`frontend/src/routes/HomePage/ShortenForm.tsx`); the disabled `ShortenPlaceholder.tsx`/`.module.css` were deleted and `HomePage` now renders `ShortenForm`. The URL field stays the visual focus per the design guide.
  - **Split into small components** (per CLAUDE.md), all under `routes/HomePage/`: `ShortenForm` (orchestrator) → `AdvancedOptions` (collapsible alias + expiry), and on success → `ResultCard` → `CopyButton` + `QrPanel`. The form swaps itself for the `ResultCard` on success and back via "Shorten another".
  - **Request state lives in a `useCreateLink` hook** (`useCreateLink.ts`): a `idle | submitting | success | error` machine that reuses the existing `createLink` client and holds an `AbortController` ref so a superseded or unmounted request can't overwrite newer state. Form field values (`url`, `alias`, `ttlSeconds`, advanced open) stay local to `ShortenForm`.
  - **Errors branch on the symbolic `reason`** via `describeCreateLinkError` (`createLinkErrors.ts`) → `{ field: "url" | "alias" | "form", message }`, so a 400 `url_*` shows under the URL field, alias `length/grammar/hyphen/reserved/taken` (400/409) under the alias field, and `rate_limited` (429) at form level. Unknown `ApiError`s fall back to the API's own message; non-API errors get a generic message.
  - **Custom alias behind an "Advanced options" toggle** (confirmed with user, over an always-visible field) — keeps the primary flow clean while still exercising the `alias_taken`/`alias_reserved` paths. The toggle mirrors `MobileNavToggle`'s `aria-controls`/`aria-expanded` pattern.
  - **Expiry selector added** (confirmed with user) — a `<select>` of 1 hour / 1 day / 7 days / 30 days mapped to `ttl_seconds`, default 30 days. Options live in `expiry.ts` (separate module so component files only export components — keeps React Fast Refresh / lint clean).
  - **QR = preview + PNG & SVG download** (confirmed with user): `QrPanel` shows the PNG via `buildQrUrl(code, "png")` on a white plate and offers PNG/SVG `<a download>` links (same-origin, so they save rather than navigate).
  - **Copy button uses the green success state** (`--color-success`): `navigator.clipboard.writeText` with a hidden-textarea `execCommand` fallback, flipping to a green "Copied!" for ~2s.
  - **CLAUDE.md compliance:** every rendered element carries a unique, descriptive `id` (form/inputs/labels, toggle, result card rows, QR image + download links); `<label htmlFor>` on every control; form error in an `aria-live="polite"` region; booleans use `is*`/`did*` prefixes.
  - **Known limitation:** `ApiError` does not expose the 429 `Retry-After` header (the Epic 14 client only parses the JSON body), so the rate-limit message is generic rather than counting down — surfacing the exact seconds would mean extending the client and is deferred.
  - **Verified:** `npm run lint` (ESLint flat config) ✓ clean; `npm run build` (`tsc -b && vite build`) ✓ succeeds (72 modules); `npm run dev` ✓ boots and serves `/` (200). Live end-to-end against the running API stack (happy path, advanced alias/expiry, and the bad-URL / taken-alias / rate-limited error cases) is left for review — there is no `docker-compose`/single-command stack bring-up in the repo yet.

## Epic 16 — Frontend: Dashboard + analytics views — COMPLETED
- **Intent:** Public paginated link list and per-link analytics rendering.
- **Scope:** Dashboard page consuming `GET /api/links` with cursor paging (newest-first, "load more"/next-cursor); per-link analytics view consuming `/analytics` (totals, daily chart, breakdowns); per-link QR download; no edit/delete (§3); unique `id`s, small components.
- **Verification:** Manual: dashboard lists newest-first and pages via cursor; opening a link shows analytics charts/breakdowns; QR downloadable. Against the running stack.
- **Depends on:** Epics 7, 8, 9, 14.
- **Implementation notes (delivered):**
  - **Confirmed product decisions (with user):** (1) **charts use `recharts`** (`^2.13.3`, installed as `2.15.4`) rather than hand-built SVG — fast to a polished result, themed to the dark palette; (2) the per-link analytics view is a **dedicated route `/dashboard/:code`** (its own shareable URL, working back button), not an inline expand/modal; (3) the dashboard link list is **card rows** that reflow to a single column on mobile, not a table.
  - **Two routes under the existing `AppShell` layout** (`App.tsx`): `/dashboard` (list) and `/dashboard/:code` (analytics). The `SiteNav` `NavLink` already uses `end={path === "/"}`, so the **Dashboard nav item stays highlighted** on the `:code` sub-route with no nav change. The analytics route is **lazy-loaded** (`React.lazy` + `Suspense`) so recharts lands in a separate ~380 kB chunk fetched only when a user opens an analytics page — the home/dashboard initial bundle stays ~189 kB (gzip 62 kB) instead of ~568 kB. This also clears Vite's 500 kB chunk warning.
  - **Dashboard list** (`routes/DashboardPage/`): a `useLinkList` hook mirrors the Epic 15 `useCreateLink` state-machine + `AbortController` pattern — it loads the first page on mount, **accumulates** pages via `loadMore()` (appends `items`, follows `next_cursor`; `null` ⇒ no more), exposes `reload()` for the error-retry, and uses the client's default limit (no `limit` param sent → server default 20). `DashboardPage` renders loading / error+retry / empty ("Shorten your first link" → `/`) / feed states; `LinkList` → `LinkCard` (one card per link: `/short_code`, a Custom-vs-Generated badge from `is_custom`, the destination truncated with ellipsis, `formatRelative(created_at)` + `formatDateTime(expires_at)`); the whole card is a `react-router` `Link` to `/dashboard/{short_code}`. "Load more" shows only while `hasMore`.
  - **Analytics view** (`routes/DashboardPage/analytics/`): `useLinkAnalytics(code)` fetches **`getLink` + `getLinkAnalytics` in parallel under one `AbortController`** (analytics carries no URLs/QR, so the detail call supplies the short URL, destination, dates, and QR code). A 404 (`ApiError.reason === "not_found"`) maps to a distinct `notFound` state → a "no link found" card; other failures → a generic error+retry. `LinkAnalyticsPage` composes small pieces: `AnalyticsHeader` (short URL + reused `CopyButton`, destination, created/expires), `TotalClicks` (headline stat in success-green), `DailyClicksChart`, five `BreakdownChart`s, and the shared `QrPanel`.
  - **Charts:** `chartTheme.ts` holds the palette **hex constants** recharts needs for inline `fill`/`stroke` (recharts can't read CSS custom properties), mirroring `tokens.css` by hand. `DailyClicksChart` is a vertical `BarChart` over the daily series; since the API series is **sparse** (only days with clicks), `fillDailyGaps()` fills the in-between UTC days with `count: 0` for a continuous timeline (empty input → empty, unparseable dates → original series). `BreakdownChart` is **one reusable horizontal `BarChart`** (`layout="vertical"`) rendered five times for `by_device_type`/`by_browser_family`/`by_os_family`/`by_referrer_domain`/`by_source`, each with a title and a "No data yet" empty state; bars arrive most-clicks-first from the API and `"unknown"` is already coalesced server-side.
  - **Shared-component extractions (per CLAUDE.md "split into smaller components"):** `QrPanel` and `CopyButton` were moved out of `routes/HomePage/` into `components/QrPanel/` and `components/CopyButton/` so the analytics view reuses them verbatim (one QR/copy UI, not two); `ResultCard` updated its imports. `CopyButton` gained an optional `elementId` prop (default `"result-copy-button"`) so its ids stay unique on whichever page renders it. Date formatting was centralized into `utils/datetime.ts` (`formatDateTime`, `formatDate`, `formatRelative`); `ResultCard`'s old inline `formatExpiry` now calls `formatDateTime`.
  - **CLAUDE.md compliance:** every authored element carries a unique, descriptive `id` (pages, sections, cards like `link-card-{short_code}`, headings, `dt`/`dd` pairs, buttons, chart wrappers, QR panel); recharts' own internal SVG nodes are library-managed (we id the container we author around each chart). Components are small and single-responsibility; booleans use `is*` prefixes (`isLoadingMore`, `is_custom`); error regions use `role="alert"`, loading uses `role="status"`, sections use `aria-labelledby`.
  - **Verified:** `npm run lint` (ESLint flat config) ✓ clean; `npm run build` (`tsc -b && vite build`) ✓ succeeds with the analytics chunk split out (no chunk-size warning); `npm run dev` ✓ boots and serves `/`, `/dashboard`, `/dashboard/:code` (200 via SPA fallback) with no import/runtime errors. Live end-to-end against the running API stack (real list paging, analytics charts populated, QR download) is left for review — there is still no `docker-compose`/single-command stack bring-up in the repo (lands in Epic 18a/18b), matching Epic 15's deferral.

## Epic 17 — Frontend: How It Works + Educational Mode + badges — COMPLETED
- **Intent:** The portfolio/educational narrative layer.
- **Scope:** Static How-It-Works page (architecture annotations, tech badges, scaling story); Educational Mode client-side toggle surfacing annotations; live numbers only from `/api/metrics` (§5.10, §14); static content in React, unique `id`s, small components.
- **Verification:** Manual: How-It-Works renders; Educational Mode toggles annotations on/off; live metrics numbers populate from `/api/metrics`.
- **Depends on:** Epics 10, 14.
- **Implementation notes (delivered):**
  - **Confirmed product decisions (with user):** (1) **Educational Mode is global**, not page-local — a header toggle whose state is shared app-wide via a React context and **persisted to `localStorage`** (key `linkshrink:educational-mode`, defaults off). It reveals annotations on How-It-Works **and** on Home/Dashboard. (2) The architecture diagram is **styled HTML/CSS boxes** (themed node cards + connectors that reflow on mobile), not an ASCII block. (3) Live metrics **auto-refresh**: fetched on mount and re-polled every 10s while the page is open, stopping on unmount.
  - **Educational Mode context** split across two files to keep the React Fast Refresh lint rule clean: `src/education/EducationalModeContext.ts` (the context object + `useEducationalMode()` hook + storage-key constant; no JSX) and `src/education/EducationalModeProvider.tsx` (the provider component, only-component export). The provider reads the stored preference on init (try/catch around `localStorage` so private-mode/blocked storage just defaults off) and writes it back in an effect. `App.tsx` wraps `<Routes>` in `<EducationalModeProvider>`.
  - **Header toggle** (`components/layout/EducationalModeToggle/`): a switch-style `<button>` using **`aria-pressed`** (on/off state, not a disclosure — distinct from `MobileNavToggle`'s `aria-expanded`), with an icon + a label that collapses to icon-only under 640px. To fit a third header control cleanly, `SiteHeader.module.css` dropped `justify-content: space-between` and pins the brand left with `margin-right: auto`, so any number of right-side controls cluster correctly and the mobile nav still wraps full-width.
  - **Two reusable primitives** (both in `src/components/`, reused site-wide): **`Annotation`** is the single gate for every educational callout — it reads `useEducationalMode()` and **returns `null` when off** (`role="note"`, accent left-border). Placing `<Annotation>` directly as a flex child means it adds no DOM (and no layout gap) when off, so the Epic 14–16 pages look unchanged by default. **`TechBadge`** is a small accent pill styled after the dashboard `LinkCard` badge.
  - **How-It-Works page** (`routes/HowItWorksPage/`): the placeholder is replaced by an orchestrator composing small section components — `ArchitectureDiagram` (tiered node cards: Nginx → API/Redirect → Postgres/Redis → Worker, with a BRD §9 caption), `SystemFlows` (redirect + analytics flows as arrow-joined step chips), `TechBadgeGrid` (the 8 BRD badges + one-line "why" each), `ScalingStory` (single-VM → instances → worker pools → distributed cache → orchestration), and `LiveMetricsPanel`. Static copy lives in sibling data modules (`techBadges.ts`, `flows.ts`, `scalingStages.ts`) so component files only export components. The 6 BRD §9 annotation topics (redirect flow, cache strategy, analytics pipeline, rate limiting, QR generation, database indexing) are placed beside their relevant sections as `<Annotation>` blocks, so the toggle visibly changes this page.
  - **Live metrics** via a `useLiveMetrics` hook (Epic 15/16 state-machine + `AbortController` pattern) that fetches `getMetrics` on mount and on a 10s `setInterval`, both cleared on unmount. A refresh keeps the last good numbers on screen (only the **first** load can fail to the error state); `LiveMetricsPanel` + `MetricStat` show cache hit ratio (as %), total redirects, queue **backlog** (`queue_pending`) vs **volume** (`queue_stream_length`), and worker health (green/healthy vs warning/stale + heartbeat age). **Accuracy note:** the endpoint does **not** expose redirect latency (TDD §5.7 — p95 is measured at Nginx, not Redis), so the panel renders the real `MetricsResponse` fields and a caption stating latency is measured at the proxy. The "Refreshing…" status line is shown only for background polls (after the first successful load) and is `aria-hidden`, so the 10s poll cadence is not announced to screen readers; the initial load and the error/retry states carry the `role="status"`/`role="alert"` regions instead.
  - **Global annotations** added inert-when-off: Home gets rate-limiting + URL-validation/SSRF callouts (after the visibility notice); Dashboard gets a keyset-pagination callout; the analytics view gets a UTC-daily-bucketing callout. Existing page logic is untouched.
  - **Removed dead code:** `components/PagePlaceholder/` — How-It-Works was its last consumer, so it is deleted now that every route is implemented.
  - **CLAUDE.md compliance:** unique descriptive `id` on every authored element (including inline `<code>` tags in annotations and each diagram node/connector); small single-responsibility components; `is*` boolean prefixes (`isEducationalModeOn`, `isRefreshing`); only existing design tokens used.
  - **Verified:** `npm run lint` (ESLint flat config) ✓ clean; `npm run build` (`tsc -b && vite build`) ✓ succeeds (home/dashboard initial bundle ~208 kB, analytics chunk still split); `npm run dev` ✓ serves `/how-it-works` and `/` (200). Live end-to-end against the running API stack (real `/api/metrics` numbers populating + 10s refresh, toggle persistence across reload) is **left for review** — there is still no single-command stack bring-up in the repo until Epic 18a/18b, matching the Epic 15/16 deferral. With the API down, the live-metrics panel correctly shows its error+retry state.

## Epic 18a — Infra: Compose core (data stores, services, migration init, health) — COMPLETED
- **Intent:** Bring up the backend half of the stack — data stores, the three Python services, the migration init step, and health checks — on an internal network, without the proxy yet. Reviewable on its own.
- **Scope:** `infra/docker-compose.yml` for `postgres`, `redis` (AOF), `api`, `redirect`, `worker`; service Dockerfiles built on the Epic 1 base image with the shared package installed editable into each; named volumes for Postgres data and Redis AOF; per-service health checks (HTTP `/health` for api/redirect, worker heartbeat script checking `metrics:worker:heartbeat` staleness ≤ 15 s per §5.2); Alembic one-shot migration init that runs and completes before the services accept traffic; **bind `api`/`redirect`/`worker` to an internal Docker network only** so they are unreachable except through the proxy added in 18b (defense-in-depth for the trusted-IP-header model, §5.9/§7).
- **Verification:** `make up` (or `infra/up.ps1` on Windows — a bare `docker compose up` fails until `linkshrink-base` is built) → postgres + redis + api + redirect + worker all reach healthy; migration init applies the schema before services start serving; api/redirect answer `/health` on the internal network; worker heartbeat health check passes and flaps to unhealthy if the worker is stopped; the three services are **not** reachable from outside the Docker network.
- **Depends on:** Epics 2, 6–13 (containerizes the API, redirect, and worker services + the migration).
- **Implementation notes:**
  - **Four new service Dockerfiles** in `infra/docker/`, each `FROM linkshrink-base` (the Epic 1 shared layer, which already has editable `linkshrink_shared`): `api.Dockerfile` and `redirect.Dockerfile` (`pip install -e services/<svc>` → `uvicorn …:app` on `8000`/`8001`), `worker.Dockerfile` (`python -m linkshrink_worker.main`, no port), and `migrate.Dockerfile`. All build with **context = repo root** so the `COPY services/…` / `migrations` / `alembic.ini` paths resolve; the existing `.dockerignore` already trims the context and excludes `.env`.
  - **`migrate` is a dedicated one-shot image** (`FROM linkshrink-base` + `pip install alembic` + the `alembic.ini`/`migrations/` copied in) running `alembic upgrade head`. Alembic is a tooling dep (`requirements-dev.txt`), not a service runtime dep, so it lives only in this image. The app services declare `depends_on: migrate: condition: service_completed_successfully`, which is the **gate** guaranteeing the schema is applied before anything serves.
  - **`infra/docker-compose.yml`** (project name `linkshrink`) defines `postgres:16`, `redis:7`, `migrate`, `api`, `redirect`, `worker` on one user-defined bridge network `linkshrink`. Redis runs with **AOF** (`command: redis-server --appendonly yes`); named volumes `postgres-data` and `redis-data` persist state.
  - **Health checks:** postgres `pg_isready`, redis `redis-cli ping`, worker `python -m linkshrink_worker.healthcheck` (heartbeat staleness ≤ 15 s, §5.2). api/redirect use a **dependency-free Python one-liner** against `/health` because the `python:3.12-slim` base has no `curl`. App services also gate on postgres/redis `service_healthy`.
  - **Decision (confirmed with user) — internal-only services, exposed data stores:** `api`/`redirect`/`worker` publish **no** ports (reachable only on the Docker network, unreachable from the host until the Epic 18b proxy — defense-in-depth for the trusted-IP-header model, §5.9/§7). Postgres `5432` and Redis `6379` **are** published to the host for local debugging (psql/redis-cli/GUI), but **bound to `127.0.0.1` only** so the (passwordless in local dev) Redis and the Postgres role are never reachable off this host.
  - **Decision (confirmed with user) — base-image build orchestration:** the service Dockerfiles `FROM linkshrink-base`, which `docker compose build` does not reliably build first. A repo-root **`Makefile`** (`make up`/`build`/`down`/`logs`/`migrate`/`ps`) and a Windows **`infra/up.ps1`** build the base image, then run `docker compose -f infra/docker-compose.yml up --build -d` — a single command for either platform.
  - **Env:** every service (including `postgres`, which reads `POSTGRES_*` to provision) uses `env_file: ../.env`; `.env.example` gains `WORKER_NUMBER=1` (names the worker's consumer in the analytics group). The worker takes `WORKER_NUMBER` straight from `.env` (no hardcoded compose override), so a second worker can be numbered by editing `.env` alone. Service names `postgres`/`redis` match the `config.py` defaults, so no host overrides are needed.
  - **No application code changed** — this epic only wires existing services together.
  - **Verified:** `docker compose -f infra/docker-compose.yml config` ✓ valid (Docker 29.4.0). Full `make up` bring-up to all-healthy is **left for review** — building the base + service images and reaching healthy is a heavier step than static validation, matching the live-stack deferral noted in Epics 15–17.

## Epic 18b — Infra: Nginx proxy, frontend build, TLS, routing — COMPLETED
- **Intent:** Wire the public edge — Nginx routing precedence, the static frontend build, TLS, and the trusted real-IP header — completing the one-command full-system bring-up.
- **Scope:** `infra/nginx/` config with routing precedence blocks 1–5 (§5.10: `/api/*`→API, `/health` & `/api/metrics`, static assets, SPA routes serve `index.html`, fallback `/{code}`→redirect), `real_ip` module (`set_real_ip_from` trusted hop + `real_ip_recursive on`) passing the single app-controlled IP header the API trusts (§5.9), TLS termination (self-signed/HTTP local, Let's Encrypt/certbot prod); add `frontend-build` (produces the static assets) and `nginx` services to the compose file from 18a; mount the built assets into Nginx. **QR `proxy_cache` (from Epic 9):** add a `proxy_cache` zone for `GET /api/links/{code}/qr` so the immutable QR image is served from the edge without re-hitting the API (the API already sets `Cache-Control: public, max-age=86400, immutable`). The `proxy_cache_key` **must include `$arg_format`** (png and svg are distinct images at the same path); honor the upstream `Cache-Control`. This is the demo-stack stand-in for a CDN edge cache in a real deployment — same header contract, so swapping in a CDN later needs no API change. **Reserved-word anti-drift:** add a scope note/checklist tying the Nginx served-path blocks (1–4) to `proxy_served_paths` in `shared/validation.py` — whenever a served path is added/changed in this config, the helper list must be updated so the Epic 4 coverage test (`reserved_words ⊇ proxy_served_paths`) keeps protecting against drift (the test compares against the helper, not the live config, §5.10/§7).
- **Verification:** `docker compose up` (full stack) → all services healthy, migrations applied; create via UI/API resolves via 302; nginx routes SPA refresh on `/dashboard` to `index.html` and bare `/{code}` to redirect (§8.1–8.2); `/api/*` reaches the API and the client IP seen by the rate limiter is the real client IP from the trusted header, not a spoofable raw `X-Forwarded-For`.
- **Depends on:** Epic 18a; Epics 14–17 (serves the built SPA). Composes the full system; can be staged but the green run needs the services it routes to.
- **Implementation notes:**
  - **Files:** `infra/nginx/nginx.conf` (http-level: `real_ip`, the `qr_cache` `proxy_cache_path` zone, `api`/`redirect` upstreams, a `log_format` printing both `$remote_addr` and `$realip_remote_addr` + `$upstream_cache_status`); `infra/nginx/conf.d/default.conf` (the 5 routing blocks, the `:80→:443` redirect + `:443` TLS server, the QR cache location, and the anti-drift checklist comment); `infra/nginx/docker-entrypoint.sh` (self-signed cert bootstrap); `infra/docker/nginx.Dockerfile` (nginx:alpine + openssl); `infra/docker/frontend-build.Dockerfile`; new compose `frontend-build` + `nginx` services and `frontend-dist`/`nginx-certs`/`nginx-cache` volumes; Makefile `frontend`/`nginx-reload`/`certs-clean` targets; `up.ps1` bring-up message.
  - **`nginx:alpine` lacks `openssl`** (confirmed at build time — base image has `apk` but no `openssl`), so the entrypoint's cert generation would fail. The `nginx` service builds `infra/docker/nginx.Dockerfile` (`FROM nginx:alpine` + `apk add openssl`) to bake it in — keeps first-boot cert generation offline/instant. Config + conf.d + entrypoint stay bind-mounted (not COPYed) so edits + `nginx -s reload` need no rebuild.
  - **TLS decision (confirmed with user): self-signed HTTPS locally, certbot/Let's Encrypt prod.** A drop-in `/docker-entrypoint.d/99-self-signed.sh` generates an RSA self-signed pair (CN/SAN from `PUBLIC_HOST`) into the `nginx-certs` volume **only if absent** — so mounting a real prod cert at the same `fullchain.pem`/`privkey.pem` paths makes the script skip and serves the real cert with **no config change**. The drop-in must not `exec nginx` (the stock launcher does) and is committed mode `100755`.
  - **Routing by match priority, not file order:** `=` exact > `^~` prefix-priority > `~` regex > plain prefix. QR cache lives in a nested `~ ^/api/links/[^/]+/qr$` under `^~ /api/links/` so it outranks the generic `location /api/`; `proxy_cache_key` is `"$scheme$host$uri?format=$arg_format"` (png/svg keyed apart) and `Cache-Control` is **not** ignored so the upstream `immutable/max-age=86400` governs. `X-Cache-Status` is exposed for verification.
  - **`/health` collision:** both api and redirect expose `/health`; `location = /health` routes the **public** one to the API per spec (block 2). The redirect service's stays internal (its own Docker healthcheck).
  - **Block 4 widened to `/dashboard/:code`:** the SPA also has the nested analytics route, so `= /dashboard` + `^~ /dashboard/` both serve `index.html`. Known SPA routes are an enumerated closed set matched with `=`/`^~`, so they always outrank the block-5 `location /` redirect catch-all (no length-based code/route guessing). Trailing-slash `/how-it-works/` would fall through to redirect, but React Router emits no trailing slash so it isn't hit.
  - **No `validation.py` change:** the served segments (`api, health, metrics, assets, static, dashboard, how-it-works`) already match `proxy_served_paths()`; the anti-drift checklist comment in `default.conf` guards future edits. **The Epic 4 coverage test still passes unchanged.**
  - **`frontend-build` is Node-based**, does not extend `linkshrink-base`; `node_modules`/`dist` are `.dockerignore`d so it `npm ci`s inside the image (`frontend/package-lock.json` is committed) and the `CMD` republishes `dist/` into the `frontend-dist` volume each run. No new base-image build step.
  - **Real client IP — nginx is the trust boundary:** locally there is no proxy in front, so the config does **not** recover the client from an inbound `X-Forwarded-For` (honoring a client-supplied XFF would let anyone forge their IP past the rate limiter). `$remote_addr` is the genuine TCP peer and each proxy location sets a single, app-controlled `X-Real-IP = $remote_addr`; the API trusts **only** `X-Real-IP`, so the client IP cannot be spoofed. In prod behind a real LB/CDN, re-enable `set_real_ip_from`/`real_ip_header`/`real_ip_recursive` scoped to that hop's egress range (**never** `0.0.0.0/0`, which would trust any peer's XFF). No HSTS on the self-signed path.
  - **Verified:** `docker compose -f infra/docker-compose.yml config` ✓ valid; `pytest tests/test_validation.py` ✓ green (anti-drift unchanged). Full `make up` bring-up to all-healthy + the live curl checklist (302 resolve, SPA refresh, real-IP recovery, QR HIT/MISS) is **left for review**, matching the live-stack deferral in 18a/15–17.
  - **Post-completion infra fix (folded into the Epic 19 working tree, recorded here for traceability):**
    - **`default.conf` — `location = /api/links` exact-match block** added ahead of the `^~ /api/links/` prefix. Without it, the prefix location's automatic trailing-slash 301 sends `/api/links` → `/api/links/`, which FastAPI (slash-less routes) then 307s back to `/api/links` — an infinite redirect loop on the bare list/create endpoint. The exact match proxies `/api/links` straight through (200, no redirect). The system tests drive the apps via in-process ASGI and never hit nginx, so this is independent of Epic 19 and belongs to the 18b edge config.
    - **`up.ps1`** — bring-up message em-dash → hyphen (cosmetic, avoids a non-ASCII glyph in the Windows console).

## Epic 19 — Integration & system tests (Testcontainers) — COMPLETED
- **Intent:** End-to-end confidence across the real PG/Redis behaviors the design depends on.
- **Scope:** pytest + Testcontainers suite (§9.9): create→redirect→analytics happy path, rate-limit 429, expiry/404 (including the cache-hit expiry AC), purge, queue recovery/DLQ, reserved-word coverage; consolidate/round out per-epic tests into a system-level suite.
- **Verification:** Full suite passes against real PG + Redis containers in CI/local.
- **Depends on:** Epics 11, 12, 13 (and the API epics they exercise).
- **Implementation notes (delivered):**
  - **Confirmed product decisions (with user):** (1) **full consolidation** — a shared `tests/conftest.py` plus refactoring all six existing integration files onto it (not just the new suite); (2) **add GitHub Actions CI** (`.github/workflows/tests.yml`) plus a `make test`/`make lint` target, rather than deferring CI wiring.
  - **New `tests/conftest.py`** holds the fixtures every integration module used to copy verbatim (~90 lines × 6): `database_url`, `redis_url`, `alembic_config` (all now **`scope="session"`**), `schema_at_head`, `session_factory`, `redis_client`, and a shared `test_settings`. pytest auto-discovers conftest, so the six files just inherit the fixtures by name with **no imports**. The six files (`test_api_links`, `test_redirect`, `test_worker`, `test_migrations`, `test_cache`, `test_queue`) dropped their local copies (and the now-unused `REPO_ROOT`/`Path`/`create_async_engine`/`async_sessionmaker`/`pytest`/`pytest_asyncio` imports), keeping only their service-specific helpers (api's `resolver_holder`/`client`/`_seed_links`; redirect's `_FailingSession`/`_client_for`; worker's seeding helpers). Net: ~360 lines of duplication removed, zero tests lost.
  - **Containers promoted from `module` to `session` scope** (the one behavior change): one throwaway Postgres and one Redis are started once for the entire suite and reused, instead of one pair per test file. **Isolation is unchanged** — `schema_at_head` still resets the DB per test (`downgrade base` → `upgrade head`) and `redis_client` still `flushdb`s per test (both function-scoped) — so this is pure wall-clock savings (full suite now ~22 s).
  - **`test_settings` standardized** on `Settings(_env_file=None, hashids_salt="integration-test-salt", public_host="link-shrink.org")`. The redirect suite's old fixture was a bare `Settings(_env_file=None)`; the salt/host additions are inert for it (it only compares the 302 `location` to the original URL), so api and redirect now share one fixture.
  - **New `tests/test_system.py`** — the cross-service suite. It wires the **API** app, the **redirect** app, and the **worker** coroutines onto the **same** shared PG + Redis (two `AsyncClient`s via `ASGITransport` + `dependency_overrides`, the redirect one `follow_redirects=False`; the worker is driven through `run_consumer_once`/`run_recovery_once`/`run_purge_once` directly, exactly as `test_worker.py` does). The API client injects a fake public-IP resolver so no real DNS/SSRF lookup happens. 7 tests:
    - **Happy path (flagship):** `POST /api/links` → 201 → redirect `GET /{code}` → 302 to the destination → `run_consumer_once` drains the queued click into a `ClickEvent` → `GET /api/links/{code}/analytics` shows `total_clicks == 1` and `by_source == [{direct, 1}]`. All three services interoperating on shared infra in one test.
    - **QR attribution:** a `?source=qr` redirect flows through to `by_source == [{qr, 1}]`.
    - **Rate-limit 429:** `RATE_LIMIT_PER_MINUTE` creates succeed, the next returns 429 + `Retry-After`.
    - **Cache-hit expiry AC (§5.6), no `sleep`:** a 120-s-lifetime link is **seeded directly** (the API clamps `ttl_seconds` to ≥3600, so it can't create one this short), warmed with one redirect, and the test asserts `0 < TTL(redirect:{code}) ≤ 120` — proving the positive entry's TTL is capped to the remaining lifetime so it can never outlive the link. The already-expired→404 path stays covered by `test_redirect.py::test_expired_code_does_not_302_even_on_warm_path`; this adds the capped-TTL angle deterministically.
    - **Purge → redirect 404:** an expired-100-days link is purged by `run_purge_once` (a 1-day-expired link is retained), after which the redirect for that code is 404 and negative-cached.
    - **Queue recovery / DLQ:** a poison click (nonexistent `link_id`) is dead-lettered to `clicks:dead` and acked out of the PEL once the delivery-attempt cap is hit (initial delivery + two reclaims).
    - **Unknown-device end-to-end:** a redirect with an empty `User-Agent` derives `device_type == unknown` all the way to the analytics breakdown.
  - **Reserved-word coverage was left where it lives** — the canonical `RESERVED_WORDS ⊇ proxy_served_paths()` assertion stays the no-container unit test in `tests/test_validation.py` (and reserved-alias→409 stays in `test_api_links.py`); the system suite does not duplicate it.
  - **CI:** `.github/workflows/tests.yml` runs on push to `main` + every PR — `setup-python@v5` (3.12, pip cache), the README editable installs (`packages/shared`, `services/{api,redirect,worker}`) + `requirements-dev.txt`, then `ruff check .` and `pytest`. **No `services:` block** — Testcontainers starts PG/Redis inside the runner's own preinstalled Docker, identical to local. `make test`/`make lint` give the same one-command entry locally.
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **200 passed** with Docker up (193 pre-existing, unchanged, + 7 new system tests) against the single shared PG + Redis session containers, ~22 s ✓; the consolidation lost no tests (behavior-neutral). Container-gated tests still skip cleanly without Docker (the shared skip guard is unchanged), so the suite exits 0 on a Docker-less machine. (Run via the project `.venv`.)

## Epic 20 — Observability & docs + load test — COMPLETED
- **Intent:** Finalize the metrics story, run instructions, and prove the latency budget.
- **Scope:** `/api/metrics` JSON shape finalized (cache hit ratio, queue depth, total redirects/throughput); README run instructions (local + VM, TLS differences); load test confirming **< 50 ms p95 cache-hit** measured server-side at Nginx via `$request_time` (§9.10); document known limitations (DNS rebinding, public abuse) from §7.
- **Verification:** README reproduces a clean bring-up; load test report shows p95 < 50 ms on cache hits measured at Nginx; metrics endpoint values match observed traffic.
- **Depends on:** Epics 10, 11, 18b (the load test measures p95 at Nginx via `$request_time`, so it needs the proxy from 18b in place).
- **Implementation notes (delivered):**
  - **Confirmed product decisions (with user):** (1) the load test ships as **tooling + a documented procedure** with the actual live `make up` run **deferred to review** — matching the live-stack deferral in Epics 15–19 (this machine has no committed full bring-up run); (2) the load generator is a **self-contained Python asyncio + httpx** script (no new binary like k6/wrk, stays in the existing `.venv` toolchain); (3) the `/api/metrics` shape is **locked at the existing 8 fields** (no new field added) — `total_redirects` *is* the throughput counter per §5.7's "no latency histogram" minimalism; (4) the §7 known-limitations writeup lives in the **README** alongside the new run instructions.
  - **No application behavior changed.** This epic is docs + a load-test harness + a one-line metrics-shape docstring. The metrics collector (`metrics.py`), schema fields, and router are untouched; only the `MetricsResponse` docstring in `schemas.py` was expanded to state the shape is **finalized (Epic 20)** and that the absence of a p95/latency field is deliberate (measured at Nginx via `$request_time`, §5.7/§9.10).
  - **Load-test harness** in new `infra/loadtest/`: `run_load_test.py` (asyncio + httpx generator — creates a link via `POST /api/links`, warms it into the Redis cache with one redirect, then fires `--requests`/`--concurrency` concurrent `GET /{code}` at Nginx with `follow_redirects=False` and `verify=False` for the self-signed cert; prints a client-side **sanity** summary and the exact parse command to run next) and `parse_nginx_p95.py` (reads the Nginx access log from stdin, keeps only `GET /{code}` `302` lines, extracts each `rt=` `$request_time`, computes p50/p95/p99 via **nearest-rank**, and **exits non-zero if p95 ≥ 0.050 s** so the budget is a hard check).
  - **Authoritative p95 is server-side at Nginx, not the client.** The generator's own timing includes localhost RTT + TLS handshakes, so it is labelled "sanity only"; the budget number comes from `$request_time` (already logged as `rt=` by Epic 18b's `nginx.conf` — **no infra change needed**). The redirect `302` carries `Cache-Control: no-store` and is **not** Nginx-cached (only QR is), so every measured line is a real end-to-end redirect served from the **Redis** cache — exactly the §9.10 "cache-hit" definition. The log is read via `docker compose exec -T nginx cat /var/log/nginx/access.log` (no log-volume mount added to compose).
  - **Pure, testable core:** `parse_request_times(lines, code)` and `percentile(values, rank)` are import-pure (the `__main__` block only wires stdin → functions → threshold), so `tests/test_loadtest_parse.py` covers the filtering (exact path compare so `/abc1234` ≠ `--code abc123`; `?source=qr` still matches; wrong method/status/code excluded), the nearest-rank math (p50/p95/p99 over 1–100), and the budget pass/fail + the `main()` exit codes (0 pass / 1 over-budget / 2 no-matching-lines) — **all with no Docker**. The script is loaded via `importlib` from its path since `infra/loadtest/` is not an installed package.
  - **Runners:** `make loadtest CODE=<code>` (Makefile target — generate then measure in one step; `REQUESTS`/`CONCURRENCY`/`CODE` overridable) and `infra/loadtest.ps1` (Windows equivalent mirroring `infra/up.ps1`). `infra/loadtest/README.md` documents the full procedure plus a fill-in **report template** for recording the observed p50/p95/p99.
  - **README expanded** (replacing the Epic-1 stub "Running the stack"): real bring-up (`make up` / `infra/up.ps1` → `make ps`/`logs`/`down`, open `https://localhost` with the self-signed warning); a **Local vs VM (TLS)** note (self-signed locally vs Let's Encrypt/certbot on the VM at the **same** cert paths = no config change; prod re-enables `real_ip` scoped to the LB hop, never `0.0.0.0/0`; only TLS + `.env` differ, §8); a **Metrics** table documenting the finalized 8-field shape; a **Load testing** section; and a **Known limitations** section covering the four §7 items (DNS rebinding, public abuse, rate-limit spoofing behind the proxy, the p95 budget). "Development setup" moved below the stack sections (de-duplicated, not lost).
  - **Verified:** `ruff check .` exit 0 ✓; full `pytest` **215 passed** with Docker up (the 14 new no-Docker parse tests + the unchanged 201 prior; container-gated tests still skip cleanly without Docker) ✓; both scripts smoke-tested without the stack (`run_load_test.py --help`; `parse_nginx_p95.py` over a sample log line → correct p95 + PASS). The **live `make up` + real load-test run / report** (and confirming metrics values match observed traffic) is **left for review**, consistent with Epics 15–19; `infra/loadtest/README.md` documents exactly how a reviewer reproduces it.
