"""linkshrink_shared — the one authoritative copy of LinkShrink's core logic.

Holds the SQLAlchemy models, short-code generation, URL/alias validation and
reserved words, env-driven config, and the Redis cache/queue helpers, so the
API, redirect, and worker services all resolve codes and read keys identically.

Modules are filled in by later epics (see each module's docstring); this scaffold
ships them empty so `import linkshrink_shared` works from Epic 1 onward.
"""

from linkshrink_shared.cache import (
    METRICS_CACHE_HIT_KEY,
    METRICS_CACHE_MISS_KEY,
    METRICS_REDIRECTS_TOTAL_KEY,
    NEGATIVE_CACHE_SENTINEL,
    NEGATIVE_CACHE_TTL_SECONDS,
    POSITIVE_CACHE_MAX_TTL_SECONDS,
    RATE_LIMIT_DAY_PREFIX,
    RATE_LIMIT_DAY_WINDOW_SECONDS,
    RATE_LIMIT_MINUTE_PREFIX,
    RATE_LIMIT_MINUTE_WINDOW_SECONDS,
    RATE_LIMIT_PER_DAY,
    RATE_LIMIT_PER_MINUTE,
    REDIRECT_KEY_PREFIX,
    CachedTarget,
    RateLimitResult,
    cache_negative,
    cache_target,
    cap_positive_ttl,
    check_rate_limit,
    decode_cached_target,
    get_cached,
    hit_window,
    increment_cache_hit,
    increment_cache_miss,
    increment_redirects_total,
    is_negative,
    ratelimit_day_key,
    ratelimit_minute_key,
    read_counter,
    redirect_key,
)
from linkshrink_shared.config import (
    Settings,
    get_redis_client,
    get_settings,
)
from linkshrink_shared.models import (
    Base,
    ClickEvent,
    DeviceType,
    Link,
    Source,
    link_code_seq,
)
from linkshrink_shared.queue import (
    CLICKS_CONSUMER_GROUP,
    CLICKS_STREAM,
    CLICKS_STREAM_MAXLEN,
    DEAD_LETTER_STREAM,
    DEAD_LETTER_STREAM_MAXLEN,
    MAX_DELIVERY_ATTEMPTS,
    WORKER_HEARTBEAT_KEY,
    WORKER_HEARTBEAT_STALE_SECONDS,
    ClickPayload,
    ack_click,
    add_click,
    claim_stale_clicks,
    dead_letter,
    deserialize_click,
    ensure_consumer_group,
    pending_count,
    read_clicks,
    read_heartbeat,
    serialize_click,
    stream_length,
    worker_consumer_name,
    write_heartbeat,
)
from linkshrink_shared.shortcode import (
    DEFAULT_MAX_ATTEMPTS,
    SHORT_CODE_ALPHABET,
    SHORT_CODE_MIN_LENGTH,
    ShortCodeCollisionError,
    ShortCodeGenerator,
    default_short_code_generator,
    fetch_next_sequence_value,
    generate_unique_short_code,
)
from linkshrink_shared.validation import (
    ALIAS_MAX_LENGTH,
    ALIAS_MIN_LENGTH,
    ALLOWED_URL_SCHEMES,
    MAX_URL_LENGTH,
    REASON_ALIAS_GRAMMAR,
    REASON_ALIAS_HYPHEN,
    REASON_ALIAS_LENGTH,
    REASON_ALIAS_RESERVED,
    REASON_URL_LENGTH,
    REASON_URL_MALFORMED,
    REASON_URL_NO_HOST,
    REASON_URL_PRIVATE_ADDRESS,
    REASON_URL_SCHEME,
    REASON_URL_SELF_REFERENTIAL,
    REASON_URL_UNRESOLVABLE,
    RESERVED_WORDS,
    HostResolver,
    ValidationError,
    default_host_resolver,
    default_public_host,
    proxy_served_paths,
    validate_alias,
    validate_url,
)

__version__ = "0.1.0"

__all__ = [
    "ALIAS_MAX_LENGTH",
    "ALIAS_MIN_LENGTH",
    "ALLOWED_URL_SCHEMES",
    "CLICKS_CONSUMER_GROUP",
    "CLICKS_STREAM",
    "CLICKS_STREAM_MAXLEN",
    "DEAD_LETTER_STREAM",
    "DEAD_LETTER_STREAM_MAXLEN",
    "DEFAULT_MAX_ATTEMPTS",
    "MAX_DELIVERY_ATTEMPTS",
    "MAX_URL_LENGTH",
    "METRICS_CACHE_HIT_KEY",
    "METRICS_CACHE_MISS_KEY",
    "METRICS_REDIRECTS_TOTAL_KEY",
    "NEGATIVE_CACHE_SENTINEL",
    "NEGATIVE_CACHE_TTL_SECONDS",
    "POSITIVE_CACHE_MAX_TTL_SECONDS",
    "RATE_LIMIT_DAY_PREFIX",
    "RATE_LIMIT_DAY_WINDOW_SECONDS",
    "RATE_LIMIT_MINUTE_PREFIX",
    "RATE_LIMIT_MINUTE_WINDOW_SECONDS",
    "RATE_LIMIT_PER_DAY",
    "RATE_LIMIT_PER_MINUTE",
    "REASON_ALIAS_GRAMMAR",
    "REASON_ALIAS_HYPHEN",
    "REASON_ALIAS_LENGTH",
    "REASON_ALIAS_RESERVED",
    "REASON_URL_LENGTH",
    "REASON_URL_MALFORMED",
    "REASON_URL_NO_HOST",
    "REASON_URL_PRIVATE_ADDRESS",
    "REASON_URL_SCHEME",
    "REASON_URL_SELF_REFERENTIAL",
    "REASON_URL_UNRESOLVABLE",
    "REDIRECT_KEY_PREFIX",
    "RESERVED_WORDS",
    "SHORT_CODE_ALPHABET",
    "SHORT_CODE_MIN_LENGTH",
    "WORKER_HEARTBEAT_KEY",
    "WORKER_HEARTBEAT_STALE_SECONDS",
    "Base",
    "CachedTarget",
    "ClickEvent",
    "ClickPayload",
    "DeviceType",
    "HostResolver",
    "Link",
    "RateLimitResult",
    "Settings",
    "ShortCodeCollisionError",
    "ShortCodeGenerator",
    "Source",
    "ValidationError",
    "ack_click",
    "add_click",
    "cache_negative",
    "cache_target",
    "cap_positive_ttl",
    "check_rate_limit",
    "claim_stale_clicks",
    "dead_letter",
    "decode_cached_target",
    "default_host_resolver",
    "default_public_host",
    "default_short_code_generator",
    "deserialize_click",
    "ensure_consumer_group",
    "fetch_next_sequence_value",
    "generate_unique_short_code",
    "get_cached",
    "get_redis_client",
    "get_settings",
    "hit_window",
    "increment_cache_hit",
    "increment_cache_miss",
    "increment_redirects_total",
    "is_negative",
    "link_code_seq",
    "pending_count",
    "proxy_served_paths",
    "ratelimit_day_key",
    "ratelimit_minute_key",
    "read_clicks",
    "read_counter",
    "read_heartbeat",
    "redirect_key",
    "serialize_click",
    "stream_length",
    "validate_alias",
    "validate_url",
    "worker_consumer_name",
    "write_heartbeat",
    "__version__",
]
